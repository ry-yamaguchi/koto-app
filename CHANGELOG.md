# 変更履歴 (CHANGELOG)

Koto のバージョンごとの変更点。新しい順。日付は YYYY-MM-DD。
分類: ✨追加 / 🔧改善 / 🐛修正 / 🔒セキュリティ / 📦配布。

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠。バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) に従う。

---

## [未リリース]

---

## 0.3.52 - 2026-08-28

- ✨ **AI の作業を待たずに、別のプロジェクトで作業を続けられるようになりました**: これまで AI が応答中は、どのプロジェクトに切り替えても入力できませんでした。これからは切り替え先で普通に送信でき、**複数のプロジェクトで同時に AI を走らせられます**（同じプロジェクト内はこれまでどおり1件ずつ）。
  - 「考えています…」「⏹ 停止」などの表示も、そのプロジェクト自身のものだけが出ます。
  - AI が作業中のプロジェクトには、一覧に **⏳** が付きます。
  - AI からの許可の確認（毎回確認モード）が同時に重なったときは、順番に1つずつ表示します。

---

## 0.3.51 - 2026-08-28

- 🐛 **AI の作業中にプロジェクトを切り替えると、返事の吹き出しが切り替え先の会話に紛れ込んで見えることがあるのを直しました**: 記録は正しいプロジェクトに残っていましたが、見た目だけ混ざることがありました。これからは切り替え先には出ず、元のプロジェクトへ戻ればそこまでの返事が見えます（「切り替えても作業が続く」形の第一歩です）。

---

## 0.3.50 - 2026-08-28

- 🔧 **🗂 会話のまとめを作っている間も、⏹ 停止が効くようになりました**: これまで「まとめています…」の間に停止を押しても何も起きませんでした。これからは作業ごと止まり、「（⏹ 停止しました）」が表示されます。
- 🔧 **🕘「元に戻す」をしたことが、会話にも残るようになりました**: 戻した直後に「🕘 ○○の時点までファイルを戻しました（会話はそのまま残っています）」と会話に記録されます。これまでは AI が「ファイルを保存した」と思ったまま実物だけが戻っており、続きの依頼で話が食い違うことがありました。

---

## 0.3.49 - 2026-08-28

- 🐛 **AI が作ったファイルが「公開されるもの（public）」の外に置かれてしまうのを直しました**: `public` フォルダのあるプロジェクトで、AI にファイルを作らせると**プロジェクトの直下**に置かれていました。そのため ①公開してもそのファイルが反映されない ②🕘「元に戻す」で戻らない、という影響がありました。これからは正しく `public` の中に保存されます。
  - コードカードの **💾 保存ボタン**からの保存も、同じように `public` の中へ入るようにしました。
  - ファイル一覧の**「公開されるもの／公開されないもの」の見出しも正しくなりました**。これまで直下に作られてしまったファイルは、この版から「公開されないもの」側に表示されます。**公開したいものは `public` の中へ移してください**（ファイルをドラッグで移せます）。
- 🐛 **⏹ 停止を押したときに「（⏹ 停止しました）」が必ず表示されるようにしました**: これまでは停止しても表示が出ず、止めたのに何かが続いているように見えることがありました（停止そのものは効いていました）。

---

## 0.3.48 - 2026-08-28

- 🔧 **内部の作りを整えました（見た目も動きも変わりません）**: 会話の記録の持ち主を、画面から裏側へ移しました。「窓を閉じても作業が続く」ようにするための下ごしらえ（0.3.45〜）の一部です。会話の保存はこれまでどおり自動で行われます。

---

## 0.3.47 - 2026-08-28

- 🔧 **内部の作りを整えました（見た目も動きも変わりません）**: AI との会話の実行を、画面の裏側へ移しました。3回に分けて進めてきた下ごしらえ（0.3.45〜）の仕上げで、この先「窓を閉じても作業が続く」「プロジェクトを切り替えても作業が続く」ようにするための土台です。

---

## 0.3.46 - 2026-08-28

- 🔧 **内部の作りを整えました（見た目も動きも変わりません）**: AI との会話の実行部分を、画面から切り離せる形にしました。前回（0.3.45）の続きで、「窓を閉じても作業が続く」ようにするための下ごしらえです。

---

## 0.3.45 - 2026-08-27

- 🔧 **内部の作りを整えました（見た目も動きも変わりません）**: チャットの画面表示を、**あとで作りを入れ替えても壊れない形**に直しました。いまは何も変わりませんが、これから進める「窓を閉じても作業が続く」ようにするための下ごしらえです。

---

## 0.3.44 - 2026-08-27

- 🔧 **長い会話でも、保存が重くならないようにしました**: これまでは、AI が返事を書いている間じゅう、**その会話ぜんぶを1.5秒ごとに書き直して**いました。やり取りが増えるほど重くなり、手元のいちばん長い会話（209件）では**毎回 5.5MB を書き直して**いました。これからは**増えたぶんだけ**書きます。同じ会話で、**返事1回ぶんが 219MB → 50KB** になりました。
  - **見た目は何も変わりません。**これまでの会話も、そのまま開けます。
  - ⚠️ **この版から会話の保存の形が変わります。**この版で一度でも会話を開くと、**0.3.43 以前へ戻したときに、その会話が見えなくなります**（消えるわけではありませんが、古い版が読めません）。戻す予定があるときは、先にお知らせください。

---

## 0.3.43 - 2026-08-26

- ✨ **さくらのAppRun で公開しているアプリを、そのまま引き継げるようになりました**: インポートのときに **【いまの公開を更新していく】** を選ぶと、**いま動いているアプリを引き継ぎます**。ここから公開すると、**そのアプリがこのプロジェクトの内容で更新され、公開のアドレス（URL）は変わりません**。イメージの置き場もそのまま使うので、**月額も増えません**。
  - ポート・環境変数・入れ物の大きさ・健康診断の場所も、**公開されていたときのまま**引き継ぎます。
  - **【別物として公開する】【まだ決めていない】** を選んだときは、これまでどおり新しいアプリになります（アドレスが変わり、月額220円・税込が上乗せされます）。**押す前にどちらになるかをお見せします。**
  - ⚠️ **秘密の値（パスワードなど）は引き継げません。** 引き継いで公開すると、いま設定されている秘密は失われます。秘密のままにしたい値があるときは【別物として公開する】を選んでください。これも押す前にお伝えします。
- ✨ **すでにこのパソコンで公開しているものには、その印が出るようになりました**: インポートの一覧に **「このパソコンの『◯◯』が公開しているものです」** と出ます。同じものを2つ持つ理由はないので、**そちらのプロジェクトを開く**ボタンも添えました（取り込めなくはしていません）。
- 🔒 **プロジェクトを削除しても、ご自身で作ったイメージの置き場を消さないようにしました**: 引き継いだプロジェクトを**フォルダごと削除**すると、**Koto が作ったのではない置き場まで消していました**。中にほかのアプリのイメージが入っていれば、それらも一緒に消えます。しかもこの画面には置き場の話が**一言も出ていなかった**ので、断る手段がありませんでした。これからは**残します**。残すことと月額220円（税込）が続くことを、削除の確認画面に出します。
  - ③公開の破棄・📡公開したもの一覧からの破棄でも、**引き継いだ置き場のときはチェックが最初から外れています**。
- ✨ **会話がいつのものか分かるようになりました**: 日が変わったところに **【今日】【昨日】【8月21日（木）】** の区切りが入ります。**吹き出しにマウスを乗せる**と、その下に時刻（**13:57**）が出ます。
  - この機能より前の会話には**日時が記録されていません**。記録が始まるところに **「日時の記録がありません」** と出るので、そこから上が「いつのものか分からない会話」です。**あとから推測で日付を付けることはしていません。**

---

## 0.3.42 - 2026-08-25

- 🐛 **日本語を入力して変換を確定しただけで、検索や保存が始まってしまうのを直しました**: 漢字に変換するときの Enter と、実行するための Enter を**区別していませんでした**。とくに**ファイル名を付けるとき**は、変換の途中の名前でファイルが作られてしまうことがありました。次の5か所を直しています。
  - ファイル・フォルダの**名前を付ける／変える**とき
  - 📚 資料の検索
  - 🌐 Webから資料を作る の検索
  - さくらのAppRun のアプリ名・アクセス制限の入力
  - 認証情報の入力欄
- ✨ **「資料を使う」をチャットのすぐそばへ移しました**: これまで 📚 資料の画面の**いちばん下**にあり、しかも**いま使っているかどうかがチャットのどこにも出ていません**でした。設定したご本人でも分からなくなります。これからは**入力欄のすぐ上**に **【📚 資料を使う／使わない】** が出て、押すだけで切り替えられます（**プロジェクトごと**の設定です）。
  - 資料をまだ登録していないときは、切り替えずに**登録の案内**を出します。
  - **【🪄 おまかせ／✋ 毎回確認】**も同じ場所へ移しました。どちらも「**送るとどう扱われるか**」の設定なので、送る場所の隣にまとめています。
- 🔧 **📚 資料の画面を整理しました**: 並びを **「① 資料を追加 → ② 登録済みの資料 → ③ 資料を検索」** にしました（初めて開いたときは一覧が空なので、追加が先にあるほうが迷いません）。
- ✨ **資料の検索で、どの資料のどこが当たったのかが分かるようになりました**: これまでは AI の回答と資料名だけでした。これからは **「見つかったところ」として、資料の名前・何番目の区切りか・当たった文章**まで出ます。**ちゃんと引けているか**を確かめるための機能なので、当てはまるものが無かったときも、はっきりそう出します。
- 🔧 **チャットの上に出ていた「開いているファイル名」を外しました**: 「AI にこのファイルが見えています」という意味で出していましたが、**AI は必要なファイルを自分で読める**ので、あの表示は実際の範囲を表していませんでした。何を読んだかは会話に出ます。

---

## 0.3.41 - 2026-08-25

- ✨ **すでに公開しているものを、そのまま取り込めるようになりました（公開先からインポート）**: 「新規プロジェクト」に **【公開先からインポート】** が加わりました。**パソコンが変わった・人から引き継いだ・ほかの環境から引っ越したい**——手元にファイルが無いときに、公開されているものから取り戻して、続きを編集できます。
  - 対応する公開先は **▲ Vercel** と **📦 さくらのAppRun** です。公開先の一覧から**ご自身で選んで**取り込みます（名前が同じというだけで勝手には結びつけません）。
  - **押す前に「このあと起きること」を全部お見せします。** 何個のファイルが入るか、公開したときに何が起きるか、取り戻せないものは何か。
  - **Vercel では「このあと、どうしますか？」を選びます。**「いまの公開を更新していく」なら次の公開でいまのページが置き換わり、「別物として公開する」なら**元はそのまま残ります**。どちらか選ぶまで進めません。
  - **さくらのAppRun では、公開すると新しいアドレスになります**（いまのアドレスは古い内容のまま残ります）。イメージの置き場が1つ増えるため**月額220円（税込）が上乗せ**されることも、押す前にお伝えします。**秘密の値（パスワードなど）は取り戻せません**ので、公開の前に入れ直してください。
  - **取り込んだ直後に 🕘 履歴の起点を作ります。** このあと何をしても、公開されていた姿へ戻せます。
  - GitHub とつないで公開しているものは取り込みません。**元のファイルは GitHub にある**ので、そちらから取ったほうが確実です。
  - 使っているキーを画面に出し、**Vercel は複数のトークンを切り替えて探せます**。「見つかりません」が「そのキーからは見えないだけ」のことがあるためです。
- 🐛 **AI に直してもらったのに、🕘 履歴に残らないことがあったのを直しました**: さくらの AI Engine で作業したとき、**退避先と履歴の見る場所が食い違って**いました。そのため「前の状態に戻す」で戻せない状態でした。**いま作業したものは、すべて履歴に残ります。**
- 🐛 **新しいプロジェクトを作った直後に、AI が前のプロジェクトのファイルを見てしまうことがあったのを直しました**: 作業フォルダの切り替えが間に合わず、**ひとつ前に開いていたプロジェクト**を見て「余計なファイルがある」と判断することがありました。
- 🐛 **作業の途中でプロジェクトを切り替えると、その作業が切り替え先に付いてきていたのを直しました**: ファイルの作成先や実行先が、**切り替えたあとのプロジェクト**に移っていました。これからは**始めたプロジェクトの中で終わります**。
- 🔒 **AI がいつもと違う場所でコマンドを実行しようとしたら、お知らせします**: 許可を求める画面に「**通常と異なる場所で実行しようとしています**」と、どのプロジェクトの話かを名前で出します。いつもどおりのときは何も出しません。
- 🔧 **公開しないプロジェクトに、フォルダの整理をすすめないようにしました**: `public` は**サーバーへ置かれるもの**の場所です。手元で動かすだけのもの（ゲームや道具）には置き先が無いので、案内しません。公開先を選んだときにお伝えします。
- ✨ **コンテナレジストリのパスワードを、見られる・コピーできるようにしました**: これまで `••••••••` のままで、**ご自身でも取り出せません**でした。この値は Koto が自動で作って保存したもので、**ほかのどこにも控えがありません**。ご自身で `docker login` してイメージを扱いたいときに必要なので、認証情報の画面（⇧⌘,）で表示・コピーできるようにしました。
- 🐛 **Vercel の「接続テスト」が、公開できない場合でも「接続OK」と出ていたのを直しました**: トークンが有効かどうかしか見ていなかったため、**公開したい先が見えていないトークン**でも緑の ✅ が出ていました。これからは公開する範囲まで確認し、見えていないときは⚠️で「チームIDを登録してください」等をお伝えします。
- 🐛 **Vercel のトークン入力欄の案内を直しました**: 決まった文字で始まるかのような例を出していましたが、Vercel 側で変わりうるため、決めつけない書き方にしました。
- 📦 **使い方ガイドを、初めて読む方の目線で全面的に見直しました**: APIキーを登録する画面の案内が誤っていたのを修正し、専門用語を減らし、目次を付けました。画像の渡し方・自分で編集する方法・公開後のURLの見かた・「素材（公開しません）」への入れ方など、書かれていなかったことを足しています。

---

## 0.3.40 - 2026-08-21

- ✨ **公開する前に、AI にひととおり見てもらえるようになりました（🛡 簡易セキュリティチェック）**: 「③ 公開」の画面に **【🛡 簡易セキュリティチェック】** が加わりました。**押したときだけ**、いま公開されるもの（`public` の中身）を AI が確認します（公開のたびに自動では走りません）。秘密情報の書き込み・危険な記述・公開すべきでないファイルの混入などを見ます。
  - **サイトかアプリかを自動で見分けて、見るところを変えます。** サイトは「置いたファイルがそのまま見える」ので**うっかり公開してしまうもの**を、アプリは「サーバーで動く」ので**訪問者の入力で危ないことができてしまわないか**を重点的に確認します。
  - 気になる点が見つかったら、**【🛠 AIに修正させる】**でそのまま直しに行けます。
  - **公開されるファイルは全部確認します。**量が多いときは何回かに分けて確認します（確認できなかったファイルがあれば、結果に名前を出します）。
  - **最後に確認した日時が残ります。**公開の画面を開き直すと「前回の確認: 8/21 20:05 ✅ 問題なし」のように出るので、**しばらく確認していないこと**にも気づけます。
  - チェックのあとに**修正するかどうかを選べます**（勝手には直しません）。**AI による簡易的な確認**なので、最終的にはご自身で確認してください。使い方ガイドに「していないこと」も書いてあります。
- ✨ **AI の利用量を使うのはどの場面かを、一覧にしました**: 使い方ガイドに「**AIの利用量を使うところ**」を追加しました。AI を呼ぶのは6つの場面だけで、それ以外（②試す・③公開・🕘 履歴・ファイルの保存）では呼びません。上限に達したときは、そのすべてを止めます。
  - `.env` などの秘密が入るファイルは、**中身を AI に送らず**、名前だけで判断します（これまでと同じ）。

---

## 0.3.39 - 2026-08-21

- ✨ **サイトの中身が `public` フォルダにまとまりました**: どのファイルが公開されるのかが、**フォルダを見るだけで分かる**ようになりました。**`public` に入れたものが、そのままサーバーへ行きます。**
  - **新しく作るプロジェクトは最初からこの形**です。
  - **以前から使っているプロジェクトは、開いたときに Koto が整理します。** 何を移すかを先にお見せします。**整理する直前の状態は 🕘 履歴に残る**ので、あとから元に戻せます。
  - AI が作るファイルも、ターミナルも、**試す・公開も、すべてこのフォルダが基準**になります。AI は**このフォルダの外へは書き込めません**。
  - AI が「フォルダの外を操作するコマンド」を実行しようとしたときは、**おまかせモードでも一度確認**します（止めはしません）。
- 🔒 **HANAMII で静的サイトを公開するとき、公開の準備用ファイルを送らないようにしました**: プロジェクトに `Dockerfile` があると、Koto が用意するものと二重になり、公開が失敗することがありました。

---

## 0.3.38 - 2026-08-20

- 🔒 **公開したサイトから、公開する必要のないファイルが読めていたのを直しました**: `Dockerfile` や `nginx.conf` は、AI が「公開の準備」のために作るファイルで、サイトの一部ではありません。それが公開先でも**そのまま読める状態**になっていました。次の公開から配信しません（`README.md` は、公開したい方がいるのでこれまでどおり公開されます）。
- 🐛 **ライトモードでも、色つきの文字が読めるようになりました（続き）**: エラーの帯や【破棄する】ボタンなど、**色の上に白い文字**を置いている場所が、暗い画面でも明るい画面でも薄くて読みづらい状態でした。「文字に使う色」と「塗りに使う色」を分けて、どちらでも読めるようにしました。
- 🐛 **📋 🗑 🔑 などの小さな文字が薄すぎたのを直しました**: 補足の文字が、暗い画面でも明るい画面でも読みづらい濃さでした。
- 🐛 **会話の画像が、黙って消えることがあったのを直しました**: チャットモードで会話の保存に失敗すると、**それまでの会話の画像がすべて外され**、しかも何もお伝えしていませんでした。これからは、外す前に **「素材（公開しません）」へ画像を書き出してから**、外したことを会話でお伝えします。

---

## 0.3.37 - 2026-08-20

- ✨ **会話が長くなっても、前に決めたことを忘れにくくなりました**: これまでは直近10往復ぶんだけを AI に渡し、**それより前は黙って捨てて**いました（実際に、95回やり取りしたプロジェクトでは**75回ぶんが消えて**いました）。長く相談するほど「さっき決めたこと」が通じなくなる原因です。これからは**捨てずに、古いやり取りを Koto がまとめてから渡します**。まとめが必要になるのは**かなり長い相談のときだけ**で、ふだんの会話では起こりません。まとめたときは会話に **「🗂 ここまでの内容をまとめました」** と出て、押せば中身も読めますし、**📋 コピー**で持ち出せます。**📄 資料として残す**を押すと、プロジェクトの「素材（公開しません）」に文書として保存できます（**アプリには使われません／公開もされません**）。**元の会話は消えません**（画面には全部残ります）。
- ✨ **【🗂 まとめる】ボタンを追加しました**: 上のまとめは**かなり長い相談になってから**しか働きません。「そろそろ区切りたい」「話がとっちらかってきた」と思ったときは、チャット上部の **【🗂 まとめる】** で自分でまとめられます（**直近3往復はそのまま残ります**）。押しても意味が無いうちは、ボタン自体を出しません。
- ✨ **どのファイルが公開されるのか、一覧で分かるようになりました**: ファイル一覧が **「公開されるもの」** と **「公開されないもの」** に分かれます（片方が空でも、両方の見出しを出します）。判定は実際に公開するときとまったく同じ仕組みを使っているので、**ここに出ているとおりのものが公開されます**。
- 🐛 **ライトモードで、色つきの文字が読めなかったのを直しました**: 「✅ すべて確認できました」「⚠️ 除外」などの色を、暗い画面用のまま白い画面に載せていたため、薄くてほとんど見えませんでした。明るい画面用の色を用意しました。
- 🔧 **公開のたびの確認をやめました**（ご指摘ありがとうございます）: 押した時点でやることは明らかなので、**新しく作るとき（費用が発生します）と、消えるとき（取り返しがつきません）だけ**確認します。
- 🔧 **古いイメージのお知らせは、溜まってから出します**: 以前は1件でも公開のたびにお伝えし、しかも「すぐに困る量ではありません」と自分で書いていました。**10件を超えてから**お知らせします（料金の内訳は、片づけの画面でご覧いただけます）。
- 🔧 **公開前のチェックは、気になる点だけ出します**: 問題が無いときは **「✅ 6項目すべて確認しました」の1行**に畳みます（内訳も開けます）。
- 🔧 **権限の上げ下げをいちいち実況しないようにしました**（ご指摘ありがとうございます）: 普段から必要最小限の権限で動いていること自体をお伝えしていないのに、消すときだけ説明するのは筋が通りませんでした。**戻せなかったときだけ**お伝えします。
- 🔒 **ゴミ箱へ送れる場所を限定しました**: これまでは、指定されたどんな場所でもゴミ箱へ送れました。プロジェクトの中など、**十分に深い場所だけ**に限りました。
- 🔒 **片づけの途中でアプリが終了しても、権限が上がったままにならないようにしました**: 次に片づけを始めるとき、何よりも先に元へ戻します。
- 🐛 **一部のモデルで、AI の返事が空になることがあったのを直しました**: 考えた内容だけを返してくるモデル（GPT-OSS など）を選んでいると、公開前のセキュリティチェックが「結果を取得できませんでした」になっていました。

---

## 0.3.36 - 2026-08-20

- ✨ **画像をアプリの部品として使えるようになりました**: これまで画像は「AIに見せる」だけで、プロジェクトには残りませんでした。画像を添付して **☑ 画像を使う** にチェックを入れて送ると、Koto が保存し、AI がそのままページに組み込みます。送ってしまったあとでも、画像の下の【📁 画像を使う】から入れられます（押すと、続きは自動で進みます）。画面のどこに落としても受け取ります。
- 🐛 **公開しても新しい内容に変わらない不具合を直しました（重要）**: 公開のたびに同じ名前でイメージを送っていたため、中身が変わっても**古いページが配られ続けて**いました。公開のたびに別の名前を付けるようにし、さらに**公開のあとに「本当に新しくなったか」を Koto が確かめる**ようにしました（「✅ 新しい内容が公開されたことを確認しました」）。
- ✨ **公開する前に、見た目の落とし穴を知らせます**: 背景画像の指定漏れ（画像がタイル状に並ぶ）、参照している画像が無い・大文字小文字が違う（**手元では見えるのに公開先で消える**）、リンク切れ、スマホ用の指定漏れ、画像の大きさ指定漏れ、使っていない画像・重い画像。**【🛠 AIに修正させる】**を押すと、チャットに移って AI がその場で直します。
- ✨ **使わなくなった画像を片づけられます**: どこからも使われていない画像を一覧で確認してからゴミ箱へ移せます（完全には消えないので戻せます）。
- ✨ **古いイメージを片づけられます**: 公開のたびに増えるコンテナレジストリのイメージを、直近5件と「いま公開しているもの」を残して片づけられます。消える一覧を確認してから実行します。
- 🐛 **AI が「やりました」と言って何もしていない場合に気づけるようにしました**: 書き込みが行われていなければ、Koto がやり直させ、それでも変わらなければ「**AI の説明の方が誤りです**」と伝えます。
- 🔧 **チャットまわりの整理**: 長いファイル名で文字が欄からはみ出す不具合を修正。入力欄の注意書きを削除。画像を重ねたときの案内をひとつにまとめ、落とさずに外へ出しても表示が残らないようにしました。
- 🔒 **二重起動の防止**: 同じ保存領域を別の Koto が使っていたら起動を止めます（設定が壊れる事故を防ぎます）。

---

## 0.3.35 - 2026-08-19

- ✨ **さくら側にあるものと、かかっている費用が一覧できるようになりました**: 「📡 公開したもの一覧」は Koto の記録なので、**記録に無いものは出ません**でした。別のパソコンで作ったものや、手で作ったものが残っていると、気づかないまま費用がかかり続けます。
  - 「💰 さくら側にあるものと、かかっている費用」を追加しました。**「↻ 調べる」を押したときだけ**さくらへ問い合わせます（何も作らず、何も消しません）。
  - **月額の合計**を大きく出します。どのキーで調べたかも表示します（キーを複数お使いの場合に、別のアカウントを見ていないか分かるように）。
  - Koto に心当たりの無いものも **❓ 付きで必ず出します**（出さなければ、放置されて課金が続くためです）。**名前が似ているだけで「あなたのもの」とは判断しません。**
  - 引けなかった種類があるときは、**黙って「0件」とは言いません**。
- 🐛 **同じ設定を2つのアプリが同時に使うと壊れる問題に、守りを入れました**: 別の場所にある Koto を同時に開くと、APIキーなどの保存が壊れることがありました。2つめは理由をお伝えして終了します。
- 🐛 **エラーの黒い画面（JavaScript error）が出ることがあったのを直しました**: Dock のアイコンをクリックしたときに、アプリの準備が終わる前に画面を作ろうとして落ちることがありました。
- 🐛 **保存された設定を読み取れないとき、「未登録」と表示しないようにしました**: 読み取れないのに「未登録」と見せると、そこへ入力し直したときに**元の設定が上書きされて消えます**。読み取れなかったことと、その理由の見当をお伝えします。

## 0.3.34 - 2026-08-18

- ✨ **ライブラリを使うアプリも公開できるようになりました**: これまでは `express` などの外部ライブラリを使っていると、**「動きません」と断って**いました。AIに「フォームを作って」と頼めば自然に出てくる作りなので、そこで行き止まりになっていました。**公開のときに Koto がライブラリを用意して、一緒に持っていきます**（Docker は引き続き不要です）。
  - 「公開できるか確かめる」に **「ライブラリ ◯件（express）も一緒に用意します」**と、かかる時間の目安が出ます。
  - **お使いのパソコン専用に作られた部品を含むライブラリは、正直にお断りします**（公開先は Linux のため、持っていっても動きません）。どのライブラリが原因かと、どうすればよいかもお伝えします。
  - 用意するのは**公開先の形**に合わせたものです。2回目からは手元の控えを使うので速くなります。
- 🔧 **AIが「npm install」を実行するとき、何が入るのかが見えるようになりました**（ご指摘ありがとうございます）: これまでは「インターネットからプログラムを取得して実行します」とだけ出ていて、**何が入るのか分からないまま**許可することになっていました。**ライブラリの名前**をお見せします。

## 0.3.33 - 2026-08-18

- 🐛 **更新の有無の判定を作り直しました**（ご指摘ありがとうございます）: 同じ資料が「最新です」と「更新されています」の**両方になる**状態でした。原因は、**さくらのAI Engineに保存した本文と、いまのページを比べていた**ことです。保存して読み戻すと本文の形が変わるため、この比べ方は最初から当てになりませんでした。
  - これからは **Koto が取ってきたページどうし**を比べます（取り込んだ時点の内容の「指紋」を手元に控えます）。間に何も挟まらないので、結果が揺れません。
  - **以前に取り込んだ資料には控えがありません。** その場合は「更新されています」とは言わず、**「更新の有無は分かりません（いまの内容を基準にしました）」**とお伝えします。次回からは正しく判定できます。
- 🔧 **全体と個別のボタンの役割を分けました**（ご提案ありがとうございます）: 同じ「更新を確認」が2箇所にあり、どちらが何をするのか分かりませんでした。
  - 上のボタン: **「↻ 更新を確認」**（全部まとめて確認）→ 更新があれば **「⬆ 全体を更新（N件）」** に変わり、まとめて最新にできます。
  - 各資料: 更新があるときだけ **「更新」** が出ます。

## 0.3.32 - 2026-08-18

- 🐛 **何度確認しても「元のページが更新されています」になるのを直しました**（ご指摘ありがとうございます）: 更新した直後でも更新扱いのままでした。比べるときに**取り込み時の見出し部分（取得日時を含む）を切り落とせておらず**、毎回違う内容として扱っていたためです（取得日時は毎回変わります）。書式が崩れていても切り落とせるようにしました。
- 🔧 **ボタンの名前を、何が起きるかで言い直しました**（ご指摘ありがとうございます）:
  - 上の「↻ 再読み込み」→ **「↻ 更新を確認」**。押すと、資料の一覧を読み直したうえで、**Webから作った資料すべての更新も確かめます**（これまでは1件ずつ「元を確認」を押す必要がありました）。
  - 各資料の「元を確認」→ **「更新を確認」**、「取り直す」→ **「更新する」**。
  - **「更新する」は、更新があるときだけ出します**（いつでも押せると、押すべきかどうかが分かりません）。
- 🔧 **更新した直後に「最新です」の表示が消えてしまうのを直しました**。

## 0.3.31 - 2026-08-15

- 🐛 **どの資料も「元のページが更新されています」になってしまうのを直しました**（ご指摘ありがとうございます）: 中身は同じなのに更新扱いになっていました。原因は2つで、**①改行の入り方の違いだけで「別物」と判定していた**（さくらのAI Engineから読み戻すと改行の形が変わります）、**②確認のときだけ取得する文字数が10分の1**で、長いページは途中で切れて比べていました。どちらも直しています。
  - **途中で切れているだけのときも「変わった」とは言いません。**
- 🔧 **押した結果が分かるようにしました**: 「取り直す」を押しても表示が変わらず、効いたのか分かりませんでした。取り直したあとは **「✅ 元のページと同じ内容です（最新です）」** と出ます。
  - 上部の **「↻ 再読み込み」** を押したときも、前の確認結果を持ち越さずに確かめ直します。

## 0.3.30 - 2026-08-15

- 🐛 **資料の出典URLを読み取れないことがあったのを直しました**: 「出典URLが記録されていないため、確認できません」と出て、**元のページの更新を確認できません**でした。取り込むときは書けているのに、**読み戻したときに形が変わっている**ためでした（箇条書きの記号が落ちる・改行が詰まる等）。目印のまわりだけを見る読み方に変え、書式の揺れがあっても読めるようにしています。
- 🔧 **それでも読めないときは、実際の中身の先頭をお見せします**: 「何が入っていたのか」が分からないと直しようがないためです。

## 0.3.29 - 2026-08-15

- 🔧 **資料の鮮度の表示を、見やすくしました**（ご指摘ありがとうございます）: 1行に詰め込んで読みにくく、警告のように見えていました。**普段は静かに、気にすべきときだけ目立つ**ようにしています。
- 🔧 **「元のページを確認できませんでした」の理由が分かるようにしました**: 何が起きたのか分からず、直しようがありませんでした。**「本文を読み取れませんでした」「出典URLが記録されていません」「元のページに届きませんでした」**を言い分けます。
- ✨ **「📡 公開したもの一覧」からも記録を片づけられるようにしました**（ご指摘ありがとうございます）: キーを失くした・公開先で先に消した等で**破棄できない**とき、記録だけが残って一覧に幽霊が並んでいました。**この一覧から消すだけで、公開したもの自体は消えません**（押す前にそうお伝えします）。
- 🐛 **破棄したのに一覧へ戻ってくることがあったのを直しました**: 記録を消しても、古い形式の手がかりが残っていると行が作り直されていました。

## 0.3.28 - 2026-08-15

- ✨ **資料が「いつのものか」分かるようになりました**（ご提案ありがとうございます）: 📚 資料に取り込んだページは、**取り込んだ時点のコピー**です。ところが一覧では、3か月前のページも今日のページも同じ顔をしていました。**AI はどちらも「いまの情報」として読みます**。
  - 各資料に **「🕘 5日前に取り込み」** のように出るようにしました。**90日を超えたもの**には ⚠️ を付けて「内容が古い可能性があります」とお伝えします。
  - Web から作った資料は、**元のページが変わっていないかを見に行きます**（資料を開いたときに自動で、数件ずつ）。変わっていれば **「🔄 元のページが更新されています」** と出ます。
  - **「取り直す」** で最新の内容に差し替えられます。**勝手には差し替えません**（押してからです）。
  - 元のページを確認できなかったときは、**「変わった」とは言いません**（相手のサイトが止まっているだけかもしれないためです）。

## 0.3.27 - 2026-08-15

- ✨ **公開したものを「見失わない」ようにしました**（ご指摘ありがとうございます）: キーを失くしたり、作り直したり、別のパソコンに移ったりすると、Koto からは公開したものを操作できなくなります。これまでは「公開済み」と表示するだけで、**そこから先へ行く道がありませんでした**（破棄もできず、記録も消せません）。無料の公開先なら散らかるだけですが、**料金のかかる公開先だと、そのまま払い続ける**ことになります。
  - 「📡 このプロジェクトの公開状況」の各行に **「管理画面を開く ↗」** を付けました。キーが無くても、公開先のサイトから確認・削除できます。
  - **「記録を片づける」** も付けました。**この一覧から消すだけで、公開したもの自体は消えません**（先に「破棄」してください）。画面にもそう書いてあります。一度押しただけでは消えません。
- 🔧 **Vercel の「公開できるか」の確認が、トークン未登録でも見えるようになりました**: 確認を「公開」の枠の中に置いていたため、**トークンを登録するまで表示されません**でした。公開先を選ぶ前にこそ知りたい情報なので、外に出しました。

## 0.3.26 - 2026-08-15

- ✨ **HANAMII へ公開しても、データがそのまま使えるようになりました**（ご提案ありがとうございます）: データは「保存場所（オブジェクトストレージ）」にあり、**アプリの置き場所とは別のところ**にあります。これまでは、その設定を渡す仕組みが**さくらのAppRun にしかなく**、同じアプリを HANAMII へ公開すると**データだけが付いてきませんでした**。
  - HANAMII の公開画面に **「💾 データの保存を持っていく」** が出ます。どの保存場所を使うのかも表示します。
  - **AppRun と同じデータを見ます。**（片方で消したものは、もう片方からも消えます）画面にもそう書いてあります。
  - 鍵は公開のたびに新しく発行し、**古い鍵はアプリが動き出したのを確かめてから**片づけます（先に消すと、動いているアプリが止まってしまうためです）。
- 🔧 **公開先ごとに鍵を分けるようにしました**: 同じプロジェクトを AppRun と HANAMII の両方へ公開したとき、**AppRun へ公開し直した瞬間に HANAMII 側が止まってしまう**問題を、起きる前に直しました。

## 0.3.25 - 2026-08-15

- ✨ **Vercel でも「公開できるか」を先に確かめるようになりました**: これまで Vercel の公開ボタンには確認が無く、**動かないアプリでも「公開成功」と表示されて**いました（画面にはソースがそのまま出てしまう状態です）。パネルを開いた時点で、押さなくても確認します。
  - 見るのは3点です。**アプリの作り**（Vercel はポートを待ち受ける常駐サーバを動かせません）／**データの保存**（いまの Koto は Vercel へ保存場所の設定を渡せないため、保存したつもりで消えます）／**公開するファイル**があるか。
  - 駄目なところには**どうすればよいか**も書きます（「さくらのAppRun か HANAMII に変えると、いまのまま動きます」など）。その場で **AIに相談する**こともできます。
  - **確認が間違っている可能性もある**ので、公開を禁止はしません。もう一度押せばそのまま公開できます。
  - 手元のファイルを読んでいるだけで、**何も作らず、何も送りません**。

## 0.3.24 - 2026-08-14

- 🔧 **画面上部のボタンの重なりを、今度こそ直しました**（ご指摘ありがとうございます）: v0.3.19 で直したつもりでしたが、**まだ重なっていました**。目で見て判断せず、幅を変えながら実際の位置を測って原因を突き止めています。
  - ウィンドウを狭くしても、「公開」「保存」「元に戻す」が真ん中の切替と**重ならなくなりました**（測定した幅 720〜1440 すべてで重なりゼロ）。
  - 「ターミナル」の文字が**縦に折れてしまうのも直しました**。
  - 狭いときに先に隠れるのは**アプリ名と版番号**です（操作のボタンは最後まで残ります）。

## 0.3.23 - 2026-08-14

- 🔧 **「動いています」と「確認できていません」が同時に出るのを直しました**（ご指摘ありがとうございます）: 「↻ 更新」で **✅ アプリが動いています** に変わっても、その上に **⏳ 起動を確認できていません** が残ったままでした。**同じ画面が二つのことを同時に言っている**状態で、どちらを信じればよいか分かりませんでした。
  - 動いていると確かめられた時点で、公開は成功しています。表示も **✅ 完了しました（公開しました。アプリが動いています。）** ひとつにまとめます。
  - **別の理由で失敗していたときは、成功にしません**（例: イメージを送れなかったとき。動いているのは前の版です）。

## 0.3.22 - 2026-08-14

- 🔧 **「アプリが起動しませんでした」の警告を、その場で消せるようにしました**（ご提案ありがとうございます）: 公開のあと Koto が起動を待つのは**最大48秒**ですが、それを超えて起動するアプリは珍しくありません。**実際には公開URLが開けているのに警告が出たまま**で、消す手立ては「もう一度公開する」しかありませんでした（公開はイメージを作り直すため、確かめるだけには重すぎる操作です）。
  - 警告の右上に **「↻ 更新」** を付けました。押すと、**いま動いているかだけ**を確かめ直します（何も作らず、何も変更しません）。動いていれば **✅ アプリが動いています** に変わります。
  - まだ準備中のときは「少し待ってからもう一度」とお伝えします。起動に失敗していたときは、これまでどおり理由と相談の導線を出します。
- 🔧 **待ち時間に間に合わなかっただけのときは「失敗しました」と書かないようにしました**: 公開そのものは終わっているのに、赤い「⚠️ 失敗しました」が出ていました。**⏳ 起動を確認できていません**という表示に変え、次にどうすればよいかを添えています。

## 0.3.21 - 2026-08-14

- 🔧 **確認で見つかった問題を、その場で直せるようにしました**（ご指摘ありがとうございます）: これまでは「公開すると直すボタンが出ます」とお伝えするだけで、**一度公開して失敗させないと直せません**でした。
  - イメージの置き場が消えていれば **「↻ 設定し直す」**、アプリの作りが合っていなければ **「AIに相談する」** が、確認したその場に出ます。
  - 直したあとは、確認をやり直します。

## 0.3.20 - 2026-08-14

- ✨ **「公開できるか確かめる」ボタンを付けました**: ③ 事前チェックから、**押す前に**さくら側の状態をまとめて確認できます（何も作らず、何も変更しません）。
  - 見るのは4点です。**イメージの置き場**が実在するか／**公開名**が空いているか／**データの保存場所**が実在するか／**アプリが起動できる作り**か。
  - 駄目なところには**どうすればよいか**も書きます。確認できなかっただけの項目では止めません。

## 0.3.19 - 2026-08-14

- 🔧 **画面上部のボタンが重なってしまうのを直しました**（ご指摘ありがとうございます）: ウィンドウを狭くすると、真ん中の「IDE／チャット」と右側の「公開」「保存」が重なり、**「公開」が「開」だけになって**いました。重ならない作りに変えています。
  - 狭いときは、**アプリ名や版番号のほうから隠れます**（操作のボタンは削りません）。

## 0.3.18 - 2026-08-14

- 🐛 **公開するたびにアプリが一時的に壊れていたのを直しました**: 公開のとき、保存場所の鍵を新しく作って**古い鍵をすぐ無効にして**いました。ですが新しいアプリが立ち上がるまでは**古いアプリが動いたまま**なので、その間に鍵を失って止まってしまいます。新しいアプリが動き出したことを確かめてから、古い鍵を片づけるようにしました。
  - 新しいアプリの起動に失敗したときは、**古い鍵をそのまま残します**（前の状態で動き続けられるように）。
- 🔧 **使われなくなった鍵が溜まらないようにしました**: これまでは1つずつしか片づけておらず、記録とずれた分が残り続けていました（消した保存場所あての鍵が残っている状態です）。公開のたびに、そのプロジェクトの古い鍵をまとめて片づけます。**ほかのプロジェクトの鍵には触れません。**

## 0.3.17 - 2026-08-14

- 🐛 **公開中に「いま何をしているか」が見えなかったのを直しました**（ご指摘ありがとうございます）: 公開には数分かかることがありますが、その間ずっと **「実行中…」としか表示されていませんでした**（進捗は画面の後ろ側に出ており、確認の画面に隠れて見えない状態でした）。
  - **「📦 イメージを組み立てています…」「🚀 AppRun に反映しています…」「🩺 アプリが動いているか確かめています…」**などが、確認の画面にそのまま出ます。
  - どの段階で止まっているかが分かるようになります。

- 🔧 **「AIに調べてもらう」がログの貼り付けを案内するようにしました**（ご指摘ありがとうございます）: Koto はログを読み取れないのに、AIへの文面は「原因を調べて」とだけ書かれており、**手がかりの無いまま相談させる形**でした。ログを貼っていただくようお願いする文面に変えました（貼れなくても、分かる範囲でお答えします）。

## 0.3.16 - 2026-08-14

- ✨ **公開したアプリのログが残るようになりました**（ご提案ありがとうございます）: これまでは**ログが残らない設定のまま**公開されていたため、アプリが動かなくても原因を調べる手がかりがありませんでした。公開のときに、ログの保存先へ自動でつなぎます。
  - **追加の費用はかかりません**（ログの保存場所がすでにある場合）。まだ無い場合は、費用のご確認をしてからご用意します。
  - ログは、さくらのクラウドのコントロールパネル（モニタリングスイート）でご覧いただけます。
  - この設定に失敗しても、**公開そのものは止めません**。

## 0.3.15 - 2026-08-14

- 🐛 **アプリが起動していないのに「完了しました」と出ていたのを直しました**: これまでは「さくらへ反映できたか」しか見ておらず、**そのあとアプリが起動できなくても成功と表示していました**。公開URLを開いて初めて気づく状態でした。
  - 公開のあと、**アプリが本当に動いているかを確かめてから**結果をお伝えします（準備中のあいだは少し待ちます）。
  - 起動できなかったときは、さくらからの理由（例:「Component is exited: ExitCode1」）をそのままお見せします。
- ✨ **起動しなかったときに「AIに調べてもらう」ボタンが出ます**: 状況とエラーをチャットに入れます（内容を確かめてから送信してください）。あわせて、**くわしいログを見る場所**（さくらのコントロールパネル）へのリンクも出します。

## 0.3.14 - 2026-08-14

- 🐛 **公開したアプリが起動しないことがあるのを直しました**: `koto-data.js` が読み取り専用（自分だけが読める）権限で置かれることがあり、**公開先で「デプロイに失敗しました」となっていました**。
  - 公開するときに、**読み取りの権限だけを足す**ようにしました。お手元のファイルの権限がどうであれ、公開先で読めるようになります。
  - **書き込みの権限は与えません。実行できるファイルはそのままです。** お手元のファイルの権限も変更しません（公開用の複製にだけ手を入れます）。
  - `.env` などの秘密のファイルは、これまでどおり公開物に含まれません。

## 0.3.13 - 2026-08-14

- 🔧 **「レジストリを設定し直す」を何度も押せてしまうのを直しました**（ご指摘ありがとうございます）: 整ったあともボタンが残っていたため、「効いていないのでは」と何度も押せる状態でした。
  - 整ったら **「✅ レジストリが整いました。もう一度『公開する』を押してください」** に変わり、ボタンは消えます。
  - 次に公開したときは元に戻るので、また必要になれば出てきます。

## 0.3.12 - 2026-08-14

- 🐛 **コンテナレジストリを削除したあと、公開できなくなるのを直しました**: コントロールパネルでレジストリを削除すると「アプリの組み立てに失敗しました」とだけ出て、**直し方が分かりませんでした**。
  - 公開の前に**レジストリが本当にあるかを確かめ**、無ければその旨をお伝えします。
  - **「レジストリを設定し直す」ボタンが出るようになりました。** これまでは Docker を使う方（エキスパート）にしか出ておらず、**標準の使い方をしている方だけが直せない**状態でした。

## 0.3.11 - 2026-08-14

- ✨ **保存場所を選べるようになりました**: 「保存場所を用意する」ときに、**すでにある保存場所から選べます**（費用は増えません）。新しく作る場合は月額が増えることを添えます。
  - 記録にある保存場所が**実際には無いとき**（削除した直後で作り直せないときなど）、その旨をお伝えして選び直せるようにしました。


- 🐛 **保存場所が作られていないのに、作れたことにしていました**: 作成の応答が「すでにあります（409）」だったとき、中身を確かめずに先へ進んでいました。**同じ名前を削除した直後は、作られていないのに 409 が返ることがあります。**
  - 作ったあとに**一覧で本当にあるかを確かめる**ようにしました。無ければその場で止め、**作成時の応答をそのままお見せします**（原因が分かるように）。
  - 公開・保存場所の用意・設定画面の「新しく作る」の3か所すべてに入れました。

## 0.3.10 - 2026-08-14

- 🐛 **保存場所を用意できずに公開が失敗するのを直しました**: 「保存場所『◯◯』を用意できませんでした（HTTP 403）」で公開できないことがありました。
  - **保存場所を作る前に、その保存場所の鍵を用意しようとしていました。** 順番を直し、**保存場所を先に用意してから**アプリを公開するようにしました。
  - 作ったばかりの鍵は**すぐに使えないことがある**ため、少し待って試し直すようにしました（それでも駄目なときは、理由をお伝えして止めます）。

## 0.3.9 - 2026-08-14

- 🐛 **失敗した破棄のあと、公開できなくなるのを直しました**: 破棄が途中で失敗すると、**すでに削除されたアプリが記録に残ったまま**になり、次に公開しようとすると「再デプロイに失敗しました（HTTP 404）」となって、公開も破棄もできなくなっていました。
  - 途中まで進んだ分は、**失敗しても記録するように**しました。記録と実際のズレが残りません。
  - 公開のときも同じです。作られたアプリが記録に残らないと、Koto から削除できないまま**費用だけがかかり続けます**。

## 0.3.8 - 2026-08-14

- 🐛 **保存場所を削除できなかったのを直しました**: 破棄しようとすると「保存場所の中身を確認できないため、削除を中止しました（HTTP 403）」となり、**保存場所が消せず月額がかかり続ける**状態でした。
  - 中身の一覧を取るときの署名の作り方が間違っていました。**書き込みは成功していたのに、一覧だけが失敗する**ため気づきにくい形でした。
  - 「確認できないなら消さない」という安全のしくみ自体は正しく働いていました（中身が分からないまま消すことはありません）。

## 0.3.7 - 2026-08-14

**アプリが動くようになりました。** これまで、公開したアプリは中身が表示されるだけで、実行されていませんでした。

- 🐛 **サーバーのあるアプリが動かなかったのを直しました**: これまで公開すると **ファイルの一覧がブラウザに出るだけ**で、`server.js` などが実行されていませんでした。データを保存するアプリは、この状態では動きません。
  - `package.json` を見て、**Node で動かすアプリなら Node で起動**します。起動するファイルは `scripts.start` から自動で判断します。
  - HTML だけのアプリは、これまでどおりそのまま配信します。
  - **外部のライブラリ（express など）を使うアプリは、まだ公開できません。** その場合は公開の前にその旨をお伝えします（黙って動かないものを公開しません）。
- 🔒 **Koto の内部ファイルが公開されていたのを直しました**: `.sakuraide.json`（公開の記録）が公開したアプリに含まれ、**ブラウザから読める状態**でした。除外するようにしました。
  - チャット履歴・過去のソース・クラウドの設定（`.sakuraide` / `.sakuraide-backup` / `.sakura-cloud`）は、以前から除外されていました。**APIキーなどの秘密は含まれていません。**

## 0.3.6 - 2026-08-14

**データの保存を、実際に使えるところまで繋ぎました。**

- ✨ **「③ 公開」から保存場所を用意できるようになりました**: データを保存するアプリで公開先を選ぶと、**「保存場所を用意する…」**が出ます。押すと金額（月額495円・税込）を確かめる画面が出て、同意すると保存場所が用意されます。
  - すでに保存場所がある場合は**それを使うので、費用は増えません**。
  - 用意したあとは「保存場所: ◯◯」と表示され、いまどこに保存されるのかが分かります。
  - 一緒に `koto-data.js` もプロジェクトに置かれるので、AIに書き直してもらう先が用意されている状態になります。
- 🐛 **公開しても保存場所が作られなかったのを直しました**: 用意する処理と公開する処理が繋がっていませんでした。
- 🔒 **身に覚えのない月額が発生しないようにしました**: これまでは、データを扱わないプロジェクトの設定ファイルにも保存場所の記述が入っていました。**費用が発生するのは、金額を見て同意したときだけ**になりました。すでにあるプロジェクトの設定はそのままで構いません（同意していないものとして扱われます）。
- 🐛 **用意した保存場所の記録が消えてしまうのを直しました**: 保存場所を用意したあとに公開の設定（使うキー・期限・公開名）を変えると、**用意した記録が消えていました**。公開しても保存場所が使われず、費用だけがかかり続ける状態になります。
- 🔧 **かかり続ける費用のお知らせを分かりやすくしました**: 公開の直後に出る費用のお知らせが、これまで**コンテナレジストリの分（月額220円）しか触れていませんでした**。保存場所についてもお伝えします。
  - **ほかのプロジェクトと共有している保存場所は、合計には足しません**（共有なので、このプロジェクト分としては出せないためです）。存在と単価（月額495円）はお伝えします。
  - **実際の請求額は、さくらのクラウドのコントロールパネルでご確認ください。** 保存した量や通信量によって変わります。
- 🔧 **破棄しても止まらない費用をお知らせします**: 保存場所は、ほかのプロジェクトが使っていたりご自身のファイルが入っていると**破棄しても残ります**（その分の月額も続きます）。破棄したあとに「何が残って、いくら続くのか」をお見せします。
- 🔧 **破棄するときに、データがどうなるかをお伝えします**: 「📡 公開したもの一覧」や「③ 公開」で破棄するとき、**保存場所のどのデータが消えて、何が残るのか**を先にお見せします。
  - **ほかのプロジェクトのデータや、ご自身で置いたファイルは消しません。**
  - 保存場所を使っているプロジェクトがほかに無ければ、保存場所ごと削除して月額を止めます。

## 0.3.5 - 2026-08-13

**公開したアプリが、データを保存できるようになりました。** 問い合わせフォームの回答や投稿の一覧など、利用者が入力したものを残せます。

- ✨ **データが消えてしまう作りを、公開前にお知らせします**: 「③ 公開」で公開先を選んだとき、そのアプリがデータを保存する必要があるかを Koto が調べてお伝えします。
  - さくらのAppRun・HANAMII・Vercel は、**アプリを作り直すたびにファイルが消えます**。これまでは動作確認では正常に見えるのに、しばらくすると「入力したデータが無くなっている」ということが起こり得ました。
  - さくらのレンタルサーバはファイルがそのまま残るため、**追加の費用は必要ありません**とお伝えします。公開先によって答えが変わります。
- ✨ **保存場所を用意できるようになりました**（さくらのオブジェクトストレージ）: **月額495円（税込）**です。すでに保存場所がある場合、プロジェクトが増えても費用は増えません。
  - **公開先を変えても、データはそのまま引き継がれます。** AppRun で作ったデータは Vercel へ移しても読めます。
  - 設定（⌘ ,）の「データの保存」で、保存場所の一覧・新しく作る・まとめて保存するかプロジェクトごとに分けるかを選べます。
- ✨ **AIがデータの保存を正しく扱うようになりました**: 保存が必要なときは Koto が用意する `koto-data.js` を使ってコードを書きます。
  - **手元で試すときは自動でパソコン内に保存**され、公開すると保存場所に切り替わります。試すために設定をする必要はありません。
  - 同時に複数の人が送信してもデータが消えないように、1件ずつ分けて保存します。

## 0.3.4 - 2026-08-12

- 🔧 **更新がうまくいかないときに、原因を調べられるようにしました**: 設定（⌘ ,）の「アプリの更新」に **「更新の記録を表示…」** を追加しました。押すと記録のファイルが Finder に表示されます。
  - これまでは更新に失敗しても「更新を確認できませんでした」と出るだけで、**なぜ失敗したのかがどこにも残っていませんでした**。
  - うまく更新できないときは、このファイルを送っていただければ原因を調べられます。
  - **記録にAPIキーやパスワードは含まれません**（書き込む前に取り除いています）。ファイルが大きくなりすぎないよう、古い分から自動で消えます。

## 0.3.3 - 2026-08-11

- ✨ **設定を、画面上部のメニューからも開けるようにしました**: 「Koto」メニューの中に **「設定…」** が入りました。ショートカットは **⌘ ,** です。
  - これまで設定（⚙️）は Koto の画面の中にしかなく、メニューから探した方が辿り着けませんでした。
  - macOS では ⌘ , が設定を開くための決まったキーなので、そこに合わせています。
- 🔧 **認証情報（APIキー）のショートカットが ⇧ ⌘ , に変わりました**: ⌘ , を設定に譲ったためです。メニュー「Koto → 認証情報（APIキー）…」と、画面右上の 🔑 ボタンからはこれまでどおり開けます。
- 🔧 **バージョンの表記を「ベータ提供中」に変えました**（これまでは「正式版前」）。

## 0.3.2 - 2026-08-11

- 📦 **ダウンロードしたインストーラ（.dmg）を、そのまま開けるようにしました**: v0.3.0 で付けた Apple の署名と公証は、**アプリ本体（Koto.app）にだけ**付いていて、**それを包むインストーラには付いていませんでした**。
  - そのため、配布を始めると **利用者が最初にする「.dmg をダウンロードして開く」でつまずく**状態でした（「Apple は……マルウェアが含まれていないことを確認できませんでした」と表示されて開けません）。
  - 中身のアプリ自体は正しく署名・公証されていたので、いったんインストールしてしまえば動作に問題はありませんでした。**入口だけが塞がっている**状態です。
  - この版から、インストーラにも署名と公証を付けています。

> ℹ️ すでに Koto をお使いの方には、この版で変わることはありません。これから配る .dmg のための修正です。

## 0.3.1 - 2026-08-10

- ✨ **アプリの更新を自動でお知らせするようにしました**: 新しい版が出ると、Koto が起動時に確認して**裏でダウンロード**します。
  - **作業中に勝手に再起動することはありません。** ダウンロードが終わっても切り替えは行わず、**次に Koto を起動したときに**新しい版になります。
  - すぐ切り替えたい場合は、設定（⚙️）の「アプリの更新」から「いますぐ再起動して更新する」を押してください。**保存していない変更があるときや、AIの応答・公開処理の途中では実行されません**（理由をお伝えします）。
  - 設定（⚙️）の「アプリの更新」から、いつでも手動で確認できます。
  - ネットワークに繋がっていないときや確認に失敗したときも、**アプリの動作には影響しません**（設定画面にその旨が出るだけです）。

> ℹ️ **この機能が実際に働き始めるのは、配布の準備が整ってからです。** それまでは設定画面に「更新を確認できませんでした」と表示されますが、異常ではありません。

## 0.3.0 - 2026-08-09

**この版から、Koto に Apple の署名と公証（Notarization）が付きます。** 配布のしかたが変わる区切りなので、バージョンを 0.3 系に上げました。

- 📦 **「開発元を確認できません」という警告が出なくなりました**: これまでは初回起動時に macOS が警告を出し、右クリック→開く といった操作が必要でした。この版からは**そのまま開けます**。
  - Apple Developer Program（Individual）の証明書で署名し、Apple の公証も通しています。
- ⚠️ **アップデート後の初回起動で、パスワードを聞かれます**（1回だけ）

  > **Koto がキーチェーン内の "Koto Safe Storage" に保存されている機密情報を使用しようとしています。**

  - **「常に許可」を押して、Mac のログインパスワードを入力してください。** 以後は聞かれません。
  - 署名が付いたことで macOS から見た Koto の「身分」が変わったため、**前の Koto が保存したAPIキーを、新しい Koto が使ってよいか**を確認しています。
  - **登録済みのAPIキーはそのまま使えます。再登録は不要です**（実機で確認済み）。
  - **「拒否」を押すとAPIキーを読めなくなります**のでご注意ください（その場合は認証情報から登録し直してください）。
- 🐛 **ウィンドウを閉じるとアプリが落ちることがある不具合を修正**: ターミナルを開いたまま画面を閉じたときや、画面を閉じた後にメニューを操作したときに、「A JavaScript error occurred in the main process」というエラーで終了してしまうことがありました。Koto は画面を閉じてもアプリ自体は動き続ける設計のため、この状態は普通に起こり得ます。
- 🐛 **インストール直後に「Claude が使えない」と表示されることがある不具合を修正**: 署名付きのアプリを macOS が初めて検証するときに時間がかかり、10秒で見切りをつけていました。待ち時間を延ばし、初回である旨をお伝えするようにしました。

## 0.2.103 - 2026-08-09

ソースコードとドキュメントの総点検を行い、**秘密情報が漏れる不具合を2件**見つけて修正しました。

- 🔒 **`.env` などの秘密ファイルが、公開したときに一緒にアップロードされていた不具合を修正**: `.env`（APIキーやパスワードを書くファイル）を公開物から除外していたのは **Vercel と GitHub保存だけ**でした。
  - **さくらのレンタルサーバ・HANAMII・さくらのAppRun の3つでは、`.env` がそのまま公開されていました。** レンタルサーバは公開フォルダの直下に置かれるため、**URLを直接叩けば中身が読める**状態でした。
  - 除外の定義を1箇所にまとめ、**すべての公開先が同じ定義を使う**ようにしました。`.env` に加えて、SSHの秘密鍵・証明書（`*.pem` など）・`.netrc` も対象です。
  - **すでに公開したことがある方へ**: 公開先に `.env` などが残っていないかご確認ください。残っていた場合は削除し、**書かれていたキーやパスワードは念のため作り直す**ことをおすすめします。
  - 環境変数は `.env` を置くのではなく、**公開先の設定画面から登録**してください（AppRun・HANAMII・Vercel とも対応しています）。
- 🔒 **公開前セキュリティチェックが、`.env` の中身をAIに送っていた不具合を修正**: チェックのために `.env` の中身（APIキーやパスワードそのもの）をさくらのAI Engine へ送信していました。ファイル名だけで「公開してはいけないファイル」と判断できるため、**中身は送らず名前だけを伝える**ようにしました。
- 🔒 **公開前セキュリティチェックの判定を厳しくしました**: 回答に「要確認」と「問題なし」が両方含まれる場合に「問題なし」と判定してしまうことがありました。**迷いがある場合は「要確認」側に倒します**。
- 🔧 **ドキュメントを実物に合わせました**: 「🕘 元に戻す」という表記を、実際のボタン名「🕘 履歴（前の状態に戻す）」に統一。README に、さくらのAppRun の**月額220円**と破棄の必要性、破棄すると公開URLが変わること、一覧やプロジェクト削除からも破棄できること、**公開物に含めないものの一覧**を追記しました。

## 0.2.102 - 2026-08-09

- 🔧 **「📡 公開したもの一覧」から破棄するときも、コンテナレジストリの名前を出すようにしました**: ③公開の破棄画面では名前を出していたのに、一覧からの破棄では出していませんでした。**見覚えのない名前ならやめられる**ようにするための表示なので、どこから破棄しても同じ情報が出るようにしました。
- 🔧 **一覧からの破棄でも、コンテナレジストリを残すか選べるようにしました**: これまでは必ず削除していました。③公開の破棄画面と同じチェック（既定は削除＝月220円の課金が止まる）にしています。残したときは結果に「課金は続きます」と出ます。

## 0.2.101 - 2026-08-09

- ✨ **プロジェクトを削除するときに、公開も一緒に破棄できるようにしました**（ご要望ありがとうございます）: これまではプロジェクトを削除しても公開はそのまま残り、しかも**「どこに何を公開したか」の記録もフォルダと一緒に消えて**いました。そのため後から Koto では止められず、コンテナレジストリの月220円（税込）が黙って続いていました。
  - 削除の確認画面に、そのプロジェクトが公開しているものを一覧で出します。
  - **「公開も一緒に破棄する」（既定オン）**で、削除の前に公開を止めます。
  - **破棄に失敗したときは、フォルダを削除しません。** 記録を失わせない方を優先します。
  - Vercel とさくらのレンタルサーバは Koto から削除できないため、どこで消せばよいかをご案内します。
- ✨ **「📡 公開したもの一覧」から直接破棄できるようにしました**（ご要望ありがとうございます）: これまでは「プロジェクトを開く → ③公開 → 該当の画面 → 破棄」と辿る必要がありました。一覧の各行から止められます。破棄できない公開先には、消す場所をご案内します。
- 🔧 **「↻ ユーザー再設定」を、必要なときだけ出すようにしました**: 常に表示していましたが、平常時に押す理由がないうえ、**押すとコンテナレジストリが新しく作られる（＝月220円がかかり始める）ことがある**ボタンでした。公開に失敗して設定し直しが必要なときだけ、そのエラーの中に出します。

## 0.2.100 - 2026-08-09

- 🐛 **公開したアプリが、別のプロジェクトのコンテナレジストリに入ってしまう不具合を修正**: プロジェクトを2つ以上あつかっていると、**あとから公開したアプリの中身が、先に準備した別プロジェクトのレジストリに保存される**ことがありました。その別プロジェクトを破棄すると、**こちらのアプリまで動かなくなります**。誤操作は必要なく、プロジェクトを行き来するだけで起きていました。
  - 公開する前に、そのプロジェクトが使うレジストリと一致しているかを確かめ、食い違っていたら公開を止めて「↻ ユーザー再設定」を押すようご案内します。
- ✨ **破棄すると公開URLが変わることを、破棄の前にお伝えするようにしました**: さくらのAppRun の公開URLはアプリごとに決まるため、**破棄して公開し直すと別のURLになり、元のURLには戻せません**。すでに誰かにURLを伝えている場合は届かなくなるので、破棄の確認画面でお知らせします。
- 🔧 **どのレジストリを使っているか分からないときに、削除のチェックを出さないようにしました**: 記録が無い場合はチェックを入れても実際には削除されないのに、「削除する」と押せてしまっていました。かわりに、コントロールパネルで確認していただくようご案内します。
- 🔧 **「↻ ユーザー再設定」が押せるボタンだと分かるようにしました**（ご指摘ありがとうございます）: 隣の見出しと地続きに見えて、押せることが分かりにくくなっていました。このボタンはレジストリが見つからないときに新しく作る（＝月220円がかかり始める）ため、はっきり見える必要があります。

## 0.2.99 - 2026-08-09

- 🐛 **「コンテナレジストリは残す」を選ぶと、そのレジストリを二度と管理できなくなる不具合を修正**: 破棄のときに「コンテナレジストリも削除する」のチェックを外すと、レジストリは残るのに**どれを使っていたかの記録が消えて**いました。そのあとは次の2つが起きます。
  - **「↻ ユーザー再設定」を押すと、残したレジストリを見つけられず新しく作ってしまう**（さらに月220円が増えます）。
  - **もう一度破棄しても「記録がないため削除していません」となり、Koto からは消せない**（さくらのクラウドのコントロールパネルで手作業で消すしかありませんでした）。
  - 残したときは名前を覚えておくようにしました。削除できたときだけ記録を消します。
- 🐛 **破棄の確認画面に、別のプロジェクトのコンテナレジストリ名が表示されることがある不具合を修正**: 画面に出す名前を「アプリ全体で1つだけ保存している接続情報」から取っていたため、**直前に別のプロジェクトで作業していると、そちらの名前が出て**いました。「見覚えのない名前ならチェックを外してください」という案内が、かえって危ない方向に働いていました。そのプロジェクト自身の記録から名前を出すようにし、レジストリを作った直後・破棄した直後にも表示を取り直すようにしました。

## 0.2.98 - 2026-08-09

- 🐛 **さくらのAppRun の「消し忘れ課金」対策が、実際には効いていませんでした**: v0.2.94〜0.2.97 で入れた対策は、「このプロジェクトがどのコンテナレジストリを使っているか」という記録が残っていることが前提でした。ところが**初めて公開したときに、その記録が消えてしまう**不具合があり、対策が一度も働いていませんでした。実機で確かめて発見しました。
  - **破棄してもコンテナレジストリが削除されず**、月220円（税込）がかかり続けていました。
  - **公開するたびにコンテナレジストリが増えて**いました（1つにつき月220円）。
  - 記録を消さないように直し、同じことが起きないようテストで固定しました。
  - **すでに公開したことがある方へ**: さくらのクラウドのコントロールパネル →「コンテナレジストリ」を開き、身に覚えのないものが増えていないかご確認ください。不要なものは削除すると課金が止まります。
- 🐛 **画面の文字に `**` という記号がそのまま出ていたのを修正**: 公開直後に出る費用の案内と、設定画面の「思考の表示」の説明で、太字にするつもりの記号がそのまま表示されていました。

## 0.2.97 - 2026-08-07

- 🐛 **破棄したのに「公開したもの一覧」から消えない不具合を修正**: さくらのAppRun で公開したものを破棄しても、③公開 の一覧に残り続けていました。すでに存在しない公開先が一覧に出ていると、何が生きているのか分からなくなります。破棄したら記録も消えるようにしました。

## 0.2.96 - 2026-08-07

- 🐛 **公開するたびにコンテナレジストリが増えていく不具合を修正**: 公開や「↻ ユーザー再設定」を押すたびに、新しいコンテナレジストリが作られていました（**1つにつき月220円（税込）**）。
  - 原因は、さくら側でレジストリを削除すると**その名前がしばらく予約状態**になり同じ名前で作り直せないため、Koto は別名（`myapp-c198` のような形）で作るのですが、次に探すときは元の名前で探していて見つけられず、また新しく作っていたことです。
  - 実際に使っている名前を覚えておいて、そちらから探すようにしました。

## 0.2.95 - 2026-08-07

- 🐛 **破棄したときに、別のプロジェクトのコンテナレジストリを削除してしまう不具合を修正（実害が出ていました）**: あるプロジェクトを破棄したところ、**直前に公開していた別のプロジェクトのレジストリが消える**という事故が起きました。
  - 原因は、削除するレジストリを「アプリ全体で1つだけ保存している接続情報」から決めていたことです。この接続情報は最後に公開したプロジェクトのもので上書きされるため、破棄が別のプロジェクトを指してしまいました。
  - 削除の対象は、**そのプロジェクト自身の記録からのみ**決めるようにしました。記録が無いときは何も削除しません。

## 0.2.94 - 2026-08-07

- ✨ **さくらのAppRun で公開したときに、かかり続ける費用を表示するようにしました**: AppRun のアプリを削除しても、イメージの置き場である**コンテナレジストリが残っていると月220円（税込・ストレージ5GiBまで）がかかり続けます**。これまで Koto はこの費用をどこにも表示していませんでした。
  - 公開URLのすぐ下に、費用と「破棄で消してください」という案内を常に出すようにしました。
  - **破棄の確認画面で、コンテナレジストリも削除するかを選べる**ようにしました（既定は削除＝課金が止まる）。レジストリ名を必ず表示するので、元から持っていたものを誤って消す心配がありません。
  - 残す選択をしたときは、結果画面でも「課金は続きます」とお伝えします。

## 0.2.93 - 2026-08-05

- 🔒 **AIがKotoの管理ファイルを書き換えられないようにしました**: これまでAIは、プロジェクトのフォルダ内であればどこにでも書き込めました。次の場所への書き込みを禁止します（**普通の作業ファイルは今までどおり自由に編集できます**）。
  - **🕘 履歴の保存場所** — ここを書き換えられると、**AIの変更を取り消す機能そのものが使えなくなります**。いちばん守るべき場所でした。
  - **チャット履歴・公開設定・クラウドの環境変数**（パスワード等が入っていることがあります）
  - **`.git` フォルダ** — ここには「保存時に自動実行されるプログラム」を置けるため、危険なコマンドの確認を回避される可能性がありました。
  - `.env` などの秘密情報のファイル
  - 3つの経路（AIのファイル保存・部分編集・AI Engineへの作業委譲）すべてに同じ制限をかけています。

## 0.2.92 - 2026-08-05

- 🐛 **利用額が実際より少なく記録されることがある不具合を修正**: AIのサービスから異常な応答（マイナスのトークン数）が返ってきた場合に、**記録済みの利用額が減ってしまう**ことがありました。上限に達して止まっていたはずのキーが、また使えるようになる可能性があります。異常な値は0として扱うようにしました。
- 🔧 **AIとのやり取りの組み立てを点検しました**: 応答が細切れで届くのを1つにまとめる処理（本文・思考・AIの作業指示）を、単体で検証できる形に切り出し、17項目のテストを追加しました。応答が途中で壊れていても処理が止まらないようにしています。**動作は今までと同じ**です。

## 0.2.91 - 2026-08-05

- 🔒 **AIが実行しようとする危険なコマンドの判定を強化**: これまで止めていなかった破壊的な操作を14種類追加しました（ファイルを消す `find -delete` や `shred`、中身を空にする `truncate`、ディスクを消す `diskutil`、権限を変える `chmod -R`、作業を捨てる `git checkout .` など）。
  - **🕘 履歴はコマンドによる削除を記録しません**（記録するのはAIがファイルを保存したとき）。そのため、この判定がすり抜けると消えたファイルは戻せません。点検して穴を塞ぎました。
  - `npm run build` や `git add .` のような**普段の作業は今までどおり止まりません**（31種類のコマンドで確認しています）。
- 🔒 **公開に含めないファイルの定義を1箇所にまとめました**: 公開先ごとに別々の除外リストを持っていたため、レンタルサーバへの公開だけ抜けが生じていました（v0.2.87 で修正済み）。同じ間違いが起きないよう、4つの公開先すべてが同じ定義を使うようにしました。

## 0.2.90 - 2026-08-05

- 🐛 **「元に戻す」を続けて実行したときに、やり直せなくなることがある不具合を修正**: ごく短い間隔で2回続けて「この時点に戻す」を押すと、1回目の「戻す前の状態」が2回目に上書きされ、**戻しすぎたときにやり直せなくなる**ことがありました。保存先が必ず別になるようにしました。
  - 自動テスト（CI）で実際に再現したもので、手動の操作では起きにくい状況でしたが、「やり直せること」は履歴機能の要なので修正しています。

## 0.2.89 - 2026-08-05

- 🐛 **🕘 履歴に表示される時刻を「AIに頼んだ時刻」に統一**: これまでは「AIが最初にファイルを書いた時刻」を表示していたため、AIの返答に時間がかかったときに、履歴の並び順と実際に戻る範囲がずれることがありました。指示を出した時刻で統一し、見出し（そのとき頼んだ内容）と時刻の意味を揃えました。
  - 以前に作られた履歴もそのまま一覧に出ます。並び順が同じ時刻で重なる場合の順序も安定させました。

## 0.2.88 - 2026-08-05

- 📦 **アプリが小さくなりました（配布 196MB → 179MB、インストール後 626MB → 533MB）**: エディタ部品（Monaco）の元ファイルが、実際には使われないまま配布物に同梱されていました。画面で動いているのは組み込み済みの方だけなので、使われない方（97MB）を外しました。**機能はまったく変わりません。**
  - 内部的には、アプリ本体のかたまり（app.asar）が 121MB → 27MB になりました。**将来アップデート機能を入れたときのダウンロード量が 1/4 になります**（更新のたびに変わるのはこの部分だけのため）。
  - エディタの表示・色分け・入力チェックが従来どおり動くことを、パッケージ版で確認しています。

## 0.2.87 - 2026-08-05

- ✨ **「前の状態に戻す」が本当に"その時点の状態"に戻るようになりました（テスターの方からの要望）**: 「3つ前のデザインの方が良かった」と思ったとき、履歴から**その時点を選べばまるごと戻せます**（それより後の変更はすべて取り消されます）。
  - これまでは「そのとき変わったファイルだけ」を戻す仕組みだったため、**3つ前に戻したつもりでも、その後で足したファイルが残って新旧が混ざる**ことがありました。
  - 履歴の一覧に、**そのときAIに頼んだ内容**が並ぶようになりました。日時だけを見て探す必要がなくなり、「3つ前のデザイン」を内容で選べます。
  - **自分でエディタを編集して保存したときも履歴に残る**ようになりました（これまではAIの変更だけでした）。短い時間の中での編集は1つにまとめて記録します。
  - 「🕘 元に戻す」を**画面上部に置きました**（これまではファイルツリーの小さな🕘だけで、気づかれていませんでした）。
  - 保存される履歴を **30件 → 50件** に増やしました。
  - 戻す前に「何ファイルが変わるか（うち何ファイルが消えるか）」を表示します。戻す直前の状態も履歴に残るので、**戻しすぎてもやり直せます**。
- 🔒 **さくらのレンタルサーバへの公開で、Kotoの内部フォルダが公開されないよう修正**: プロジェクト全体を公開する構成（`public/` フォルダが無い場合）で、**チャット履歴（`.sakuraide`）と上記の履歴（`.sakuraide-backup`）がサーバの公開領域にアップロードされていました**。除外するようにしました。他の公開先（HANAMII・AppRun・Vercel・GitHub保存）では以前から除外済みです。
  - すでに公開済みの方は、サーバ上の `~/www/.sakuraide` と `~/www/.sakuraide-backup` を削除してください（次回の公開では自動では消えません）。

## 0.2.86 - 2026-08-03

- ✨ **考えているAIの「思考」が見えるようになりました（ユーザー要望）**: Kimi K2.7 のような"考えてから答える"モデルは、答えを書き始めるまで数十秒沈黙することがあり、画面には「⏳ 時間がかかっています…」としか出ませんでした。**待っている間、AIがいま何を考えているかをそのまま表示**するようにしました（🧠 の折りたたみ。クリックで開閉できます）。
  - 既定では**考えている間だけ開き、答えを書き終えると自動で畳みます**（本文の邪魔をしないため）。設定（⌘ ,）の「AIの思考の表示」で、常に開いたままにもできます。
  - **待ち時間の秒数**も表示するようにしました（あと少し待つか、止めるかの判断に使えます）。
  - あわせて、思考が届いている間は「⏳ 時間がかかっています」と出ないよう直しました（**進んでいるのに止まったように見えていました**）。
  - 思考の内容はAIに送り返さず、チャット履歴にも保存しません（費用と保存サイズを増やさないため）。考えを出さないモデルでは表示されません。なお**思考は英語で書かれることがあります**（モデルが内部で使う言葉のため。回答そのものは日本語です）。

## 0.2.85 - 2026-08-01

- 🐛 **「Node.js を入れているのに `npm` が見つからない」問題を修正（ユーザー報告）**: Dock やFinderからKotoを起動すると、パソコンに入れたコマンド（`node` / `npm` / `docker` など）をKotoが見つけられないことがありました。macOSでは、アイコンから起動したアプリに渡される「コマンドの探索場所（PATH）」が最小限になり、Homebrew などで入れたコマンドの場所が含まれないためです。起動時に、ターミナルと同じ探索場所を読み込むようにしました。
  - 影響していたのは3か所です: **AIがコマンドを実行するとき**（「`npm: command not found`」と出ていました）、**ターミナルパネル**、そして**公開前の前提チェック**です。
  - 特に前提チェックでは、**Docker を入れているのに「Dockerが必要です」と案内される**可能性がありました（さくらのAppRunへの公開時）。
  - ターミナルパネルもログインシェルで起動するようにしました（`~/.zprofile` に書かれた設定が反映されます）。

## 0.2.84 - 2026-07-31

- ✨ **「📡 公開したもの一覧」を追加（ユーザー要望）**: メニューの「表示」→「公開したもの一覧…」から、**これまでKotoから公開したものを全プロジェクト横断で一覧**できます。公開先ごとにまとまり、新しい順に並びます。各行から公開URLを開く・コピーする、そのプロジェクトを開くができます。
  - **公開先のサービスに障害が出ているときでも開けます**（APIキーもネットワークも使わず、手元の記録だけを読むため）。「あのサービスに何を公開していたっけ」をすぐ確認できます。
  - プロジェクトを開いていなくても見られます。
  - **これはKotoに残っている記録で、サービスの現在の状態ではありません**。サービス側で削除しても記録は残り、別のパソコンや管理画面から公開したものは表示されません。画面にその旨を明示し、各サービスの管理画面へのリンクを併記しています。

## 0.2.83 - 2026-07-31

- ✨ **新規プロジェクトの作成が「チャットにお願いする」方式になりました（ユーザー提案）**: これまでは作成ダイアログの中でAIが雛形を作り終えるまで待つ必要があり、**何が起きているか見えず、失敗しても言い直せません**でした。「作成」を押すとすぐにプロジェクトが開き、**実際のファイル作りはIDEのチャットが担当**します。
  - **作業の様子がそのまま見えます**。うまくいかなければ「やり直して」「続けて」と日本語で言い直せます（普段のチャットと同じです）。
  - 一度の応答に全ファイルを詰め込む必要がなくなったため、**長い出力の途中で切れて失敗することがなくなりました**。ツールが使えないモデルを選んでいるときの自動切り替えなど、チャット側の機能もそのまま効きます。
  - ダイアログの「このあとチャットで作業するAI」で、さくらのAI Engine / Claude と使うモデルを選べます（APIキーを登録済みのものだけ表示）。**この選択はそのままチャットの頭脳・モデル設定に反映されます**（前バージョンでは作成時だけの一時的な選択でしたが、チャットが作業を担当するようになったため一致させています）。
  - APIキーが1つも登録されていない場合は、これまでどおり最小限のローカル雛形と案内ファイルだけを作成します。

## 0.2.82 - 2026-07-31

- ✨ **新規プロジェクトを作るAIを選べるようになりました（ユーザー要望）**: これまで新規プロジェクトの雛形は、Claude頭脳モードで使っていても**必ずさくらのAI Engineが作成**していました。新規作成画面に「作成を担当するAI」を追加し、**さくらのAI Engine と Claude のどちらに作らせるか、さらに使うモデルまで選べる**ようにしました。
  - APIキーを登録しているAIだけが選択肢に出ます。選んだ内容は次回の初期値として覚えます。
  - この選択は**その作成のときだけ**に使われ、チャットの頭脳設定は変わりません。
  - **Claudeのキーだけを登録している場合、これまでAIによる雛形生成が使えませんでした**（無言でローカルの雛形になっていました）。今回から使えます。
  - 生成に失敗したときは従来どおりローカルの雛形で作成します（中途半端なフォルダは残りません）。Claude の利用額は従来どおり「さくらのAI Engineとは別枠」で月次に加算されます。

## 0.2.81 - 2026-07-31

- 🔧 **③公開が「最後に公開した公開先」の画面で開くようになりました（ユーザー要望）**: これまでは、別の公開先へ公開したあとでも、最初に選んだ公開先の画面が開いていました。前回公開した先の画面がそのまま開くので、続けて公開するときの操作が減ります（まだ一度も公開していないプロジェクトは、従来どおり設定された公開先の画面が開きます）。
- 🐛 **さくらのAppRunで公開しても、プロジェクトの公開先が切り替わらない問題を修正**: レンタルサーバ・HANAMII・Vercel は公開すると「このプロジェクトの公開先」が更新されるのに、**AppRunだけ更新されていません**でした。そのため、AppRunへ公開したあともAIには別の公開先向けの前提が渡り続けていました。他と同じ動きに揃えています。

## 0.2.80 - 2026-07-30

- ✨ **AIが「ファイルの一部だけを直す」ようになりました（ユーザー要望）**: これまでAIは、既存ファイルを1行変えるだけでも**全文を書き直して**保存していました。長いファイルほど時間・費用がかかり、書き出しの途中で切れて失敗することもありました（推論型モデルで実際に発生）。変更したい箇所だけを置き換える方式に対応し、**速く・安く・失敗しにくく**なりました。上書き前の自動バックアップ（🕘 履歴から復元）は従来どおりで、「✋ 毎回確認」モードでは編集前に確認します。
- ✨ **AIがプロジェクト内を横断検索できるようになりました**: 「この文字はどこにある？」を調べるのに、これまではファイル一覧を見て1つずつ全文を読み込んでいたため、時間がかかり「ツール実行の回数上限」に達しやすくなっていました。**1回の検索で該当ファイルと行番号が分かる**ようになり、無駄な読み込みが減ります（`node_modules` などの重いフォルダ・画像等のファイルは自動で除外）。

## 0.2.79 - 2026-07-30

- 🔧 **「新しいモデルを選んでいるのに古いモデルに切り替わる」問題を修正（ユーザー報告）**: `preview/Kimi-K2.7-Code` を選んでいても、ファイル操作などツールが必要な場面で「Kimi K2.6 に切り替えて実行します」と古いモデルに移ってしまっていました。原因は、**モデルがツールを使えるかどうかをモデル名のパターンで決め打ち**していたことです（新しいモデルは名前が似ているだけで「非対応」と判定されていました）。
  - **実際に使ってみた結果を覚える方式に変更**しました。まだ確かめていないモデルは、まずそのまま使ってみます。ツールが正しく動けば以後もそのまま使い、サーバー側が非対応を返したときだけ「このモデルは非対応」と記憶して、対応モデルへの切り替えを行います。
  - **新しいモデルが増えても、Kotoを更新せずに正しく判定**されます。記憶は30日で見直します（さくらインターネット側があとから対応することがあるため）。
  - あわせて、切り替え先には「使えることが確認済みのモデル」を優先するようにしました。

## 0.2.78 - 2026-07-30

- ✨ **頭脳（Claude / さくらのAI Engine）の切り替えをモデル選択の横にも追加（ユーザー要望）**: これまで切り替えは画面右下と設定画面の中だけにあり、モデルを選ぶ場所から離れていました。チャットのモデル選択の隣に「🧠 Claude ⇄」「🧠 AI Engine ⇄」のボタンを置き、その場で切り替えられるようにしました（IDEモードのチャット・チャットモードの両方）。右下・設定のどこで切り替えても表示は連動します。両方のキーを登録している場合だけ切り替えでき、片方だけのときは現在の頭脳の表示になります。

## 0.2.77 - 2026-07-29

- ✨ **Claudeのモデル一覧を自動で最新化（ユーザー要望）**: これまで選べるモデルはKoto側に**埋め込み**で、Anthropic のラインナップが変わるたびにKotoの更新を待つ必要がありました。起動時に、**お使いのAPIキーで実際に提供されているモデルを取得して一覧にする**ようにしました（さくらのAI Engine 側と同じ仕組み）。新しいモデルが出ても、Kotoを更新せずにその場で選べます。
  - キー未登録・オフライン・取得失敗のときは、前回取得した一覧、それも無ければ代表的なモデル（Fable 5 / Opus 5 / Sonnet 5 / Haiku 4.5）を表示します。
  - 選んでいたモデルが提供終了していた場合は、自動で使えるモデルに切り替えてその旨をお知らせします（さくらのAI Engine 側と同じ動き）。
- 🔧 **利用額が取得できなかったときの表示を正直に**: 返信末尾の利用額が `$0.0000` と出ると「無料だった」と誤解されるため、金額を取得できなかった場合は「利用額を取得できませんでした」と表示するようにしました（Koto側が知らない新しいモデルを使ったときに起こり得ます）。

## 0.2.76 - 2026-07-29

- ✨ **Claudeのモデルを最新のラインナップに更新（Opus 4.8 → Opus 5）**: Anthropic の提供モデルが世代交代し、Opus 4.8 は旧世代（レガシー）になりました。選べるモデルを **Fable 5 / Opus 5 / Sonnet 5 / Haiku 4.5** に更新しています。料金は Opus 4.8 と Opus 5 で同じ（$5/$25 per 1M トークン）です。これまで Opus 4.8 を選んでいた場合は、次回から自動的に Opus 5 が使われます（設定のやり直しは不要）。
- 🔧 **Claude連携の土台（Agent SDK）を更新**: 同梱している Claude 連携部品を 0.3.206 → 0.3.220 に上げました。**古いままだと Opus 5 の料金表を知らないため、画面に出る利用額が正しく計算されない**おそれがあったための更新です。APIキーの再登録は不要です。

## 0.2.75 - 2026-07-23

- ✨ **処理の実行中にウィンドウを閉じようとすると確認するように**: これまでは、AIが応答中・公開処理中・VPS操作中・プロジェクト作成中でも、✗ボタンで閉じると確認なしにその処理が中断されていました。実行中に閉じようとすると「処理を実行中です。いま閉じると中断されます。よろしいですか？」と確認し、うっかり中断してしまうのを防ぎます（未保存の編集がある場合の確認も従来どおり出ます）。
- ✨ **公開が途中で中断されたときに次回お知らせ**: 公開の最中にアプリを閉じる・失敗するなどで完了しなかった場合、Koto側は「未公開」と表示していても、さくら側では公開が完了していることがあります。この食い違いを検知し、次に③公開を開いたときに「前回、○○への公開が完了前に中断された可能性があります。実際に公開されたかご確認ください」とお知らせするようにしました。

## 0.2.74 - 2026-07-23

- ✨ **新規プロジェクトに「📄 まっさら（空で始める）」を追加**: これまで新規プロジェクトは種別（Webサイト／アプリ）を選ぶ必要があり、必ず `index.html` などの雛形ファイルが作られていました。まだ何を作るか・どこに公開するか決めていない場合のために、**ファイルを一切作らずに空で始める**選択肢を追加しました（ユーザー要望）。選ぶと公開先や要望の入力欄も出さず、作りたいものが決まったらあとからAIチャットで頼んで作れます。

## 0.2.73 - 2026-07-23

- 🔧 **「ツール実行の回数上限に達しました」がすぐ出る問題を大幅に改善**: AIが自分で作業できる回数の上限が小さく（IDEモード5回・チャットモード3回）、ファイルを数個読んで直して確認するだけで打ち切られていました。上限を引き上げ（IDEモード25回・チャットモード12回）、普通の作業なら最後まで走り切るようにしました。
  - **暴走への歯止めを追加**: 上限を上げた代わりに、AIが**まったく同じ操作を繰り返し始めたら自動で中断**するようにしました（無駄な費用と時間を防ぎます）。停止ボタンと月間予算の上限も従来どおり効きます。
  - **「▶ 続ける」ボタンを追加**: それでも区切りに達したときは、ボタン1つで続きから再開できます（これまでは「続けて」と手で入力する必要がありました）。あわせて打ち切り時の文言を、失敗のように見えない表現に変えました。

## 0.2.72 - 2026-07-23

- ✨ **さくらのVPS 公開機能の第一歩（接続）を追加**（開発中）: ③公開に「さくらのVPS（開発中）」を追加し、**VPSへ安全に接続できるところまで**を実装しました。接続方法は2つ用意しています。
  - **推奨（新しいVPS・作り直してよい場合）**: Kotoが接続用の鍵を作り、**初期設定スクリプト**を生成します。それをさくらのコントロールパネルの「マイスクリプト」に貼り付け、OS再インストール時に選ぶだけ。**Kotoがパスワードを一度も扱わずに**接続できるようになります。
  - **既存のVPSを消したくない場合**: ホスト・ユーザー・初回パスワードを入力すると、鍵を設置します。**鍵で繋がることを確認できてから**パスワード認証を無効化するので、締め出される心配がありません（パスワードは保存しません）。
  - サーバの身元（ホスト鍵）を初回に記録し、次回以降に変わっていたら**接続を中断して警告**します（なりすまし対策）。
  - まだ「接続」までです。サーバの初期構築とサイトの公開は次のバージョンで追加します。

## 0.2.71 - 2026-07-17

- ✨ **HANAMII と Vercel の認証情報にも「🔌 接続テスト」を追加**: これまで さくらのAI Engine・Claude・GitHub のキーにしか接続テストが無く、HANAMII と Vercel は「公開してみるまでトークンが正しいか分からない」状態でした。登録直後にその場で有効性を確認できるようにしました（どちらも読み取りだけの確認で、何も作成・課金しません。Vercel は個人アカウントならチームID欄は空欄のままで確認できます）。

## 0.2.70 - 2026-07-17

- 🐛 **Vercel公開でボタンが「公開中…」のまま戻らなくなる問題を修正**: 公開処理が失敗したときにボタンの状態を戻す処理が抜けており、失敗経路によってはボタンが押せないまま固まって見えることがありました。どんな結果でも必ず元に戻すように修正。あわせて、公開の完了確認が時間内にできなかった場合は「成功」と誤表示せず「まだビルド中の可能性があります。Vercelの管理画面でご確認ください」と案内し、状態確認が繰り返し失敗する場合は5分待たずに中断して原因を表示するようにしました。
- ✨ **Vercel公開の進捗を表示**: これまで公開中は「公開中…」としか出ず、数十秒〜数分かかる間ずっと無反応に見えていました。「ファイルを収集中」「アップロード中 (3/19)」「Vercelでビルド中…（◯秒）」のように、いま何をしているかを表示するようにしました。
- 🐛 **公開URLが二重に `https://` になる問題を修正**: 公開成功時のURLが `https://https://…` となりリンクが開けない状態だったのを修正。

## 0.2.69 - 2026-07-17

- ✨ **公開先に Vercel を追加**: ③公開の「さくら以外の公開先」から Vercel を選べるようになりました。トークンを登録すれば、プロジェクトのファイルをアップロードして公開URLを取得できます（静的サイト／フロントエンド向け。常駐サーバは不可・データは国外）。認証情報に「Vercel」カードを追加（トークンは https://vercel.com/account/tokens で発行。個人アカウントはチームID欄を空欄に）。「🔰 初めて公開する方へ」に手順と注意を用意しています。
  - 仕組み: 各ファイルを SHA1 付きで Vercel にアップロード（`POST /v2/files`）→ デプロイ作成（`POST /v13/deployments`）→ 公開されるまで状態を確認して公開URLを表示。トークンはお使いのパソコン内にのみ暗号化保存され、公開のたびに使う瞬間だけ読み出します（他のキーと同じ方式）。

## 0.2.68 - 2026-07-17

- 🐛 **📚 資料モーダルの高さがタブによって変わる問題を修正**: 「Webから資料を作る」タブだけ中身が少ないためモーダルが縮み、タブボタンの位置が切り替えのたびに上下にずれていました。タブ表示時はモーダルの高さを固定し、どのタブでもボタン位置が揃うように（APIキー未登録時の案内表示は従来どおり小さいまま）。

## 0.2.67 - 2026-07-17

- 🔧 **既定のテーマをライトモードに変更**: 初回起動時（テーマ未選択時）はライトモードで表示するように。すでにテーマを切り替えたことがある場合は、その選択がそのまま優先されます。あわせて起動直後のウィンドウ背景も白に合わせ、ライト利用時に一瞬暗い画面が見える現象を解消。

## 0.2.66 - 2026-07-16

- ✨ **「📦 さくらの資料パック」を追加**: 📚 資料モーダルの新タブから、さくら公式ドキュメントの厳選セット（🌐 レンタルサーバ8ページ／⚙️ AppRun 5ページ／🤖 AI Engine 5ページ／🌸 HANAMII 1ページ・全19ページ、取得可能なことを確認済み）を**ボタン一つ**で資料に一括登録できます。進行表示・失敗分のみ再試行・再取り込み時は旧パックを確認つきで置き換え。パックのURLは週次のリンク切れ検査（CI）の対象に追加。
- ✨ **費用の目安を画面に表示**: 資料パックのタブに「💰 費用の目安」を表示。資料の全文がAIに送られることはなく、会話1回に追加されるのは関連する抜粋だけ（最大3件×2,000字。既定モデルで最大約0.2円）であることを、実装の定数から自動計算して示します。READMEにも同じ説明を追記。
- 🐛 **Webページ取り込みが12,000字で切れていた問題を修正**: 「Webから資料を作る」（および新しい資料パック）で、長いページが約12,000字で切り詰められて資料化されていました。この上限は「AIの会話に渡す用」のもので、資料の書庫への取り込みには不要だったため、取り込み経路のみ上限を大きく（20万字）しました。AIの会話でURLを読む動作は従来どおりです。

## 0.2.65 - 2026-07-16

開発計画（docs/roadmap.md）フェーズ1: フィードバック導線・ガイド追随・小粒の負債解消。

- ✨ **メニューに「フィードバックを送る…」を追加**: ヘルプメニューから、気づいた点や不具合を報告するページ（GitHub Issues）をブラウザで開けます。本文にはアプリのバージョンとmacOSのバージョンが自動で入ります（APIキー等の秘密情報は含みません）。
- ✨ **HANAMII: 「🔄 再起動して反映」を追加**: 環境変数・ヘルスチェックの変更だけなら、ビルドし直す「再公開」を待たずに、アプリの再起動だけで反映できるようになりました（公開済みプロジェクトで表示。数十秒の停止をともなうため確認つき）。コードも変更した場合は従来どおり「再公開」をお使いください。
- 🔧 **使い方ガイドに「Claudeを"頭脳"として使う」を追加**: アプリ内ガイド（ヘルプ → 使い方ガイド）にClaude頭脳モードの説明がありませんでした。登録手順・切り替え方・料金が別枠であることを追記。あわせてガイドの古い記述を修正（公開先にHANAMIIが抜けて「2種類」のままだった／Claudeキーの登録先の案内が実際の画面と違っていた／権限モードのボタン絵文字）。
- 🔧 **モデル能力プローブの測定条件を改良**（開発者向け・scripts/probe-models.mjs）: 出力上限が小さすぎて、思考してから答える推論型モデルが「答える前に打ち切られて」能力なしと誤判定されうる問題を修正（上限512に引き上げ・トークン消費量の記録・「測定不能」の区別を追加）。
- 🔧 **内部整理**: 危険コマンド判定が2ファイルに複製されていたのを1箇所（src/shared）に一本化（判定基準の食い違い事故を予防）。旧リポジトリ名を指す未使用の定数を削除。

## 0.2.64 - 2026-07-16

ライセンスの明示と、ドキュメントの整備。

- ✨ **Apache License 2.0 で提供することを明示**: `LICENSE` と `NOTICE` を追加し、アプリにも同梱するようにしました。これまで利用条件が存在しなかったため、配布物を受け取った方にとっての権利が不明確でした。無保証（AS IS）である点と、`NOTICE` に記載した「個人提供の非公式ツールである」旨は、派生物にも引き継がれます。
- 🐛 **ライセンス表示にKoto自身が混ざっていた問題を修正**: 「第三者OSSライセンス」の一覧から自分自身を除外する処理が、旧名 `sakura-ide` をハードコードしていたため、Koto へ改名した時点で効かなくなっていました。結果としてKoto自身が一覧に載り、ライセンス条文の欄にREADME全文が表示されていました。改名で再発しないよう、package.json の名前を参照するように変更しています（表示は558件→557件）。
- 📦 **README に未記載だった仕様を追記**: Claude頭脳モード（3モード構成・切替・利用不可時の案内・実装委譲）、さくらのAI Engine のモデル別対応表（ツール／画像・実測値）、GitHub保存、前の状態に戻す、チャット履歴の保存。
- 📦 **旧名「Sakura IDE」と「社内」表記をドキュメントから一掃**: 個人提供であることと矛盾する表記が残っていたものを修正しました。
- 📦 **過去バージョンのタグを整備**: v0.2.12〜v0.2.63 のタグが未作成だったため、CHANGELOG に基づいて遡って作成しました（全65バージョン）。

## 0.2.63 - 2026-07-14

実測（scripts/probe-models.mjs・verify-vision.mjs）に基づくモデル対応の更新。

- ✨ **GPT-OSS 120B でもファイル参照（ツール実行）が使えるように**: 実測でツール呼び出しに対応していることを確認し有効化（従来はQwen3-Coder系とKimi K2.6のみ）。推論型モデルのため、v0.2.62で入れた出力上限の引き上げと切り詰め対策がそのまま効きます。
- ✨ **Kimi K2.6 が画像を直接読めるように**: 実測で画像入力に対応していることを確認。画像を添付したとき、これまでは「画像対応モデルで読み取り→本来のモデルで実行」の2段階処理を挟んでいたが、Kimi K2.6 を選んでいる場合はKimi自身が直接読むように（速度・精度の向上）。
- 🔧 **llm-jp はツール非対応のまま**: 実測でサーバー側がツール呼び出しに未対応（400）と確認。文章生成用として引き続きご利用ください（ファイル参照が必要な依頼では、対応モデルへの切り替えを案内します）。

## 0.2.62 - 2026-07-14

- 🐛 **大きめのファイルを書き込むとエラー（400 Unterminated string）で失敗する問題を修正**: AIの一度の出力上限（4096トークン）が小さく、特に思考してから答える推論型モデル（Kimi K2.6 など）ではファイル全文を書く途中で出力が途切れ、壊れた指示がサーバーに送られて失敗していた。出力上限を大きく引き上げ（16384トークン。対応できないモデルは自動で調整）、これで多くのファイル書き込みが通るように。あわせて、それでも収まらないほど大きい場合は、壊れた内容をサーバーへ送らずに安全に中断し「内容が大きすぎます。変更を小さく分けるか、別のモデル（Qwen3-Coder など）でお試しください」と分かりやすく案内するように。

## 0.2.61 - 2026-07-14

- ✨ **Kimi K2.6 でもファイル参照（ツール実行）が使えるように**: これまでファイルの読み書き等のツール実行はQwen3-Coder系のみだったが、さくらのAI Engineの Kimi K2.6（プレビュー）が構造化されたツール呼び出しに対応していることを実測で確認し、有効化。モデル一覧にも「Kimi K2.6（プレビュー）」の表示名を追加。
  - 補足: Kimiは推論型モデルのため、回答が思考（reasoning）側に出た場合も本文として表示する既存の仕組みがそのまま効きます。画像添付時は（K2.6の画像入力が未確認のため）従来どおり画像対応モデルで読み取ってから実行する2段階処理になります。指示への追従はQwen3-Coderよりも弱い場合があるため、既定モデルは引き続きQwen3-Coder 480Bを推奨。

## 0.2.60 - 2026-07-14

- 🔧 **アプリ内に残っていた🌸絵文字を新しいロゴマークに統一**: サイドバーの初期画面・AIチャットの見出し・チャットの空画面などに、Unicodeの桜絵文字（🌸）がロゴのように使われて残っていた（「以前のさくらのアイコンが残っている」ように見える原因。ユーザーの指摘で判明）。アプリの新ロゴ（淡い桜の一輪）に置き換え。HANAMII（サービス名）の🌸と、生成されるサイトのテンプレート内の🌸は従来どおり。
- 🔧 **プロジェクトが有るときの初期画面をすっきりと**: プロジェクトが既に存在するのに「プロジェクトを選んでください＋大きな新規作成ボタン」の初期セットアップ風ブロックが表示されていた（仕様だったが分かりにくいとの指摘）。プロジェクトが有るときは一覧を主役に表示し、「＋新規プロジェクト（AI）」「📂 既存のフォルダを開く」は一覧の下の控えめなボタンに変更。初期セットアップ風の画面はプロジェクトが1つも無いときだけに。

## 0.2.59 - 2026-07-14

- 🐛 **プロジェクト一覧が一時的に「まったくない」表示になることがある問題を修正**: 一覧の読み込みが一時的に失敗した場合に空の一覧として表示していた。失敗時は直前の一覧を維持するように（プロジェクトが存在するのに空に見えない）。
- 🔧 **チャットの自動スクロールを会話領域の内側に限定**: ブラウザ標準の自動スクロールはウィンドウ全体を動かしうるため、会話領域の内側だけをスクロールする方式に変更。あわせて、何かがウィンドウ全体のスクロール位置を動かしても即座に元へ戻す保険を追加（表示ずれの予防措置）。

## 0.2.58 - 2026-07-14

- 🐛 **開いているプロジェクトを削除すると空フォルダとして「復活」する問題を根本修正**: 削除（ゴミ箱移動）自体は成功していたが、直後にチャット履歴の保存処理が走り、削除済みの場所にフォルダを作り直して書き込んでいた（復活したフォルダの中身はチャット履歴だけ）。もう一度削除すると消えたのはこのため。プロジェクトフォルダが存在しない場合はチャット履歴を保存しないようにした。実機の自動再現テストで「削除→一覧からもディスクからも消えたまま」を確認済み。

## 0.2.57 - 2026-07-14

- 🐛 **半透明の色指定が全アプリで効いていなかった問題を根本修正**: エラー・警告バブルの背景色、ホバー時の色、淡い枠線など「色に不透明度を付けた指定」（16ファイル・76箇所）が、テーマ色の定義方式の問題でビルド時に黙って無効化されていた。ダークモードでは偶然読めていたが、ライトモードで「白文字が背景なしで乗って読めない」原因になっていた（HANAMIIログ問題と同系統）。色定義をTailwind標準の方式（RGB三つ組＋alpha対応）に移行し、全箇所が意図どおり描画されるように。あわせて全画面のライトモード可読性を総点検（固定の明色文字が明るい背景に乗る箇所は他に無いことを確認）。

## 0.2.56 - 2026-07-14

- 🐛 **Dockのアプリアイコンに白い四角の背景が残っていた問題を修正**: アイコン画像の生成時に角丸の外側の透明部分が白で塗られていた（レンダリング手段の制約）。透明を保持できる方法で作り直し、Dockで角丸アイコンとして正しく表示されるように。

## 0.2.55 - 2026-07-13

- 🔧 **アプリ内のロゴ（桜の一輪）を柔らかい雰囲気に**: 単色フラットで硬い印象だった花のマークを、アプリアイコンと同じ淡いパステル調（薄い花弁を重ねた二層＋グラデーション＋白い芯）に変更。

## 0.2.54 - 2026-07-13

- ✨ **アプリアイコンを「満開の桜と散る花びら」に刷新**: 淡いピンクから空色へのグラデーションに、満開の桜の天蓋と舞い散る花びら。アプリ内のロゴマークも同じ意匠の桜の一輪に統一。
- 🐛 **削除したプロジェクトが一覧に残って見えることがある問題を修正**: プロジェクト削除の直後、削除前に読み取られた古い一覧が画面を上書きして「復活したように見える」ことがあった。一覧の読み込みに世代管理を入れて古い結果を破棄し、削除完了後にディスクの実態で一覧を確定するように。表示に残った項目をもう一度削除してもエラーにならず綺麗に消える。
- ✨ **チャットモードでも右下で頭脳を切り替え可能に**: IDEモードと同じ「● Claude ⇄／● さくらのAI Engine ⇄」チップをチャットモードの右下にも表示（両方のキー登録時にクリックで切替）。
- ✨ **委譲のAI Engine予算ガード**: Claudeモードで、さくらのAI Engineの月間利用額が上限に達している場合は作業の委譲を行わず、その旨をチャットに表示（上限超過後も委譲経由で課金が続くのを防止）。
- 🐛 **HANAMII: 公開直後の「⚠ アプリが応答していません」を抑制**: 公開処理中や、起動直後でヘルス情報が未反映（unknown）の段階では赤い警告を出さず「動作確認中…」の中立表示に（実際の異常時のみ警告）。
- 🐛 **HANAMII: ログがライトモードで読めなかった問題を修正**: ログ表示の背景色クラスが効いておらず白文字が透明背景に乗っていた。端末風のダーク背景に修正し両テーマで読めるように。
- 🔧 **HANAMII: 公開名の上限を実測に合わせ45文字に**（2026-07-13 実測で45字の公開成功を確認）。

## 0.2.53 - 2026-07-13

- 🐛 **削除したはずのAPIキーが復活する問題を修正**: さくらのAI Engineのキーを認証情報ダイアログで削除・保存しても、アプリを再起動すると復活していた。原因はキーが2箇所（認証情報の中央ストアと、アプリ内部の旧保存位置）に保存されており、起動時に旧位置の複製から埋め戻されていたため。認証情報の中央ストアを「正」とし、旧位置の複製は起動時・削除時に自動で掃除するように。削除したキーは再起動後も削除されたままになる。

## 0.2.52 - 2026-07-13

- 🔧 **APIキーの呼び方を「アカウントトークン」に統一**: キー入力欄の例示が「Bearer トークン」という技術用語のままだったのを、さくらのAI Engine のコントロールパネル上の呼称「アカウントトークン」に合わせた（オンボーディングと認証情報ダイアログ）。コンパネからコピーする値の名前と画面の案内が一致し、迷わなくなる。
- ✨ **オンボーディングから他のAPIキー登録へ直行できるように**: 接続テスト成功後に「🔑 他のAPIキーも登録する（Claude・GitHub など）」ボタンを追加。押すとキーを保存したうえで認証情報ダイアログが開き、初期セットアップでまとめてキーを登録できる（従来は「最初のプロジェクトを作る」一択だった）。

## 0.2.51 - 2026-07-13

- 📦 **アプリ名を「Sakura IDE」から「Koto」へ変更（個人提供化）**: 本アプリは個人が開発・提供する非公式ツールであり、さくらインターネット株式会社の製品ではありません。同社のサービス（AI Engine・AppRun・レンタルサーバ 等）に接続・公開する機能は従来どおり利用できます。
  - アプリ名・ウィンドウタイトル・About・配布物名・アプリ識別子（appId）を「Koto」系に変更。
  - ロゴ／アプリアイコンを独自デザイン（チャットの吹き出し＋きらめき）に差し替え。
  - 著作権表記を「© 2026 meryo」に変更。オンボーディング・README・使い方ガイドに「個人提供・非公式」の注記を追加。
  - さくらの各サービスへの言及・リンク（連携先の事実）は維持。
  - ⚠️ **この更新後、登録済みのAPIキーの再入力が一度必要です**（暗号化がアプリ識別子に紐づくため）。設定（モデル選択・上限など）もリセットされます。作成済みプロジェクト（~/SAKURAIDE）とチャット履歴（各プロジェクトの .sakuraide/）は保持されます。

## 0.2.50 - 2026-07-13

- ✨ **画面右下の頭脳表示をクリックで切り替えられるように**: 右下の「● Claude／● さくらのAI Engine」の表示を、ClaudeとさくらのAI Engineの両方のキーを登録しているときはクリックで切り替えられるように（⇄ マーク付き）。Claudeが使えず一時的にさくらのAI Engineへ切り替えた後、Claudeが復旧したらここをクリックするだけでClaudeに戻せます（設定→チャットの頭脳 でも切り替え可能）。

## 0.2.49 - 2026-07-13

- 🐛 **Claude利用不可時の切り替え提案が出なかった不具合を修正**: v0.2.48で追加した「さくらのAI Engineに切り替えて続ける」提案が、実際の請求エラー時に出ず通常のエラー文になっていた。Claudeの請求エラーはアプリ内部で日本語（「請求設定に問題があります…」）に変換されてから表示されるため、英語のキーワードだけを見ていた判定に一致していなかった。日本語の文言も検出するように修正（一時的なレート制限や通信エラーは引き続き対象外）。※ 提案ボタンは さくらのAI Engine のキーを登録している場合に表示されます。

## 0.2.48 - 2026-07-13

- ✨ **Claudeが使えないとき、さくらのAI Engineへの切り替えを提案**: ClaudeのAPIキーは登録済みでも、クレジット不足や請求設定の問題で使えないことがある。その場合に「Claudeが今は使えないようです。さくらのAI Engineに切り替えて、同じ内容をもう一度送りますか？」というボタン付きの案内をチャットに表示するように。ボタンを押すと頭脳をさくらのAI Engineに切り替えて（設定でいつでもClaudeに戻せます）、同じ依頼をそのまま送り直します。さくらのAI Engineのキーが無い場合は従来どおりAnthropicの請求設定の確認を案内します。

## 0.2.47 - 2026-07-13

ユーザビリティ総点検の第4弾（仕上げ）: 細かな要改善・使い勝手の底上げをまとめて。

- 🐛 **認証情報（APIキー）を保存前に閉じても消えないように**: キーを入力・貼り付けた後、保存せずに画面外クリックや✕で閉じると警告なく消えていた。未保存の変更があるときは「破棄して閉じますか？」と確認するように。
- ✨ **隠しファイルの表示切り替えを追加**: `.htaccess`・`.gitignore` など「.」で始まる公開設定ファイルがファイル一覧に一切出ず編集できなかった。ファイル欄の 👁 ボタンで表示を切り替えられるように（既定は非表示、アプリ内部フォルダは常に非表示のまま）。
- ✨ **公開URL・保管場所URLにコピーボタンを追加**: AppRun／HANAMII の公開URL、GitHub の保管場所URLをワンクリックでコピーできるように（LINEやメールでの共有が楽に）。
- 🔧 **開発ツール（Homebrew）のインストール前に確認を表示**: これまで確認なしで実行していたインストール操作に確認ステップを追加。
- 🔧 **GitHub保存で名前がかぶったときも代替名をワンクリック提案**: 公開（AppRun／HANAMII）と同じく、保管場所の名前が既に使われている場合に別名候補を提案して作り直せるように。
- 🔧 **表記・分かりやすさの改善**:
  - GitHub保存ボタンに「💾 保存」のラベルを追加（アイコンのみで気づきにくかった）。
  - 「🚀 公開」ボタンの説明を「レンタルサーバ / AppRun / HANAMII」に更新。
  - ファイル一覧の 🕘 を「保存前の状態に戻す」と明確化（チャット履歴と混同しないように）。
  - AI／ターミナルの表示切り替えボタン、長いファイル名にマウスを乗せたときの説明（ツールチップ）を追加。
  - 「準備中」のさくらのVPS認証欄にその旨を明記。
  - Claude が内部の作業ツールを使っているときの表示を日本語に（英語のツール名がそのまま出ていた）。委譲中の表示を「さくらのAI Engineに作業を任せています」に、チャットの「おまかせ」を 🪄 に変更（委譲の 🤝 と区別）。
  - 画像以外のファイルを添付しようとしたら「画像ファイルのみ添付できます」と案内。
  - 設定の単価一覧を内部モデルIDから分かりやすい表示名に。
  - 最初のあいさつ生成に失敗しても技術的なエラー文を出さないように。

## 0.2.46 - 2026-07-13

ユーザビリティ総点検の第3弾: Claude利用の「見える化」と会話の継続性。

- ✨ **チャットの頭脳（Claude / さくらのAI Engine）を設定から切り替えられるように**: これまでは両方のキーを登録するとClaudeが自動で優先され、切り替えるにはキーを消すしかなかった。設定（⚙️）に「チャットの頭脳」を追加し、両方のキーがあるときはワンタップで切り替え可能に（次のメッセージから反映）。片方だけのときは現在どちらで動いているかを表示。
- ✨ **今月のClaude利用額を設定画面に表示**: Claudeの実際の利用額（$）と円の概算を「今月の利用状況」の下に表示。Anthropicへ直接課金され、さくらのAI Engineの月間上限（¥）とは別枠であることを明記。任意で「警告の目安額（USD）」を設定でき、超えると警告のみ表示（送信はブロックしない）。
- 🔧 **Claude利用の同意ダイアログに料金の説明を追加**: 初回の同意確認に「Claudeの利用料金が別途発生する（Anthropicへ直接課金・さくらのAI Engineの月間上限とは別枠）」ことと「設定で さくらのAI Engine に切り替えられる」ことを明記。
- ✨ **Claudeとの会話の続きが再起動後も通じるように**: これまではアプリを閉じたりプロジェクトを切り替えるとClaudeが直前の会話を忘れ、画面には履歴が残るのにAIには通じない状態だった。プロジェクトごとに会話（セッション）を記憶し、次回も続きから頼めるように。記憶が失効していた場合は自動的に新しい会話として続行する（エラーで止まらない）。

## 0.2.45 - 2026-07-12

ユーザビリティ総点検の第2弾: エラー表示を「分かりやすい一文＋次の行動」に統一（生のAPI応答・ログは「詳細を見る」の折りたたみに降格し、原因究明用に残す）。

- 🔧 **さくらのAI Engineの接続テスト失敗が英語のまま出ていた問題を修正**: キーが正しくないとき等に、OpenAIライブラリの英語エラーがそのまま初回設定画面に出ていた。「APIキーが正しくないようです」「インターネット接続を確認してください」等の日本語案内に変換（GitHub/Claudeのテストと同じ扱いに揃えた）。
- 🔧 **HANAMII公開の失敗メッセージから生JSONを分離**: 「公開に失敗しました（HTTP 400）: {…}」のようにAPIの生応答が本文に混ざっていたのを、人間可読な理由を本文に、生の応答は「詳細を見る」の折りたたみに。
- 🔧 **AppRunのビルド失敗が生ログの行き止まりになっていた問題を改善**: 「よくある原因（package.jsonの記述ミス／存在しないライブラリ名など）＋AIに相談もできる」という案内を主表示にし、ビルダーの生ログは「詳細を見る」に。
- 🔧 **公開名の重複・作成上限の案内カードと生エラーの二重表示を解消**: 親切カードが出るときは、元の技術的なエラー文を折りたたみに降格してカードを主役に。
- 🔧 **Claudeモードのエラー案内を分離＋レート制限・会話超過に対応**: Claude利用時のエラーで「さくらのAI Engineのキーを確認」と誤案内していたのを「Claudeのキー／Anthropic Console」へ。混雑時（429）は「少し待って再送」、会話が長すぎるときは「クリアして続きを依頼」、クレジット不足時は「Anthropic Consoleで請求設定を確認（さくらのAI Engineに切り替えても続けられる）」と案内。

## 0.2.44 - 2026-07-12

ユーザビリティ総点検（全30項目）の第1弾: バグ4件の修正。

- 🐛 **【重要】ClaudeのAPIキーだけを登録した場合（モードB）にチャットが使えなかった問題を修正**: チャット画面が「さくらのAI Engineのキーを登録してください」で止まり、Claudeが使える状態なのに到達できなかった。どちらかのキーがあればチャットを表示するように。単独チャット（プロジェクトを開いていない画面）ではClaudeのツールが使えないため、その場合は「IDEモードでご利用ください」と案内して行き止まりを防止。Claude利用同意をキャンセルした場合も、フォールバック先が無いときは「送信を中止しました」を明示。
- 🐛 **チャットのAI表示が実動作とずれていた文言を修正**: ステータスバーの「Claude（画像添付時は さくらのAI Engine）」は旧動作の説明のままだった（現在は画像もClaudeが直接処理）。空のチャット画面の「さくらのAI Engineに質問してください」等もClaudeモード時は「Claude」に出し分け。画像の2段階処理の注記もClaudeが直接処理するターンでは非表示に。
- 🐛 **公開失敗時に読めない生JSONが表示されることがある問題を修正**: さくらのクラウドAPIのエラー応答がネスト形式（`error.errors[0].message`）の場合に人間可読メッセージを取り出せず、JSON全文がそのまま画面に出ていた（例: AppRunアプリ作成上限のエラー）。具体的な理由文を優先して抽出するように。
- 🐛 **公開名の重複検出が発火しないケースを修正**: エラーメッセージに埋め込まれた「（HTTP 409）」表記を重複と判定するようにし、判定パターンに「既に」「duplicat」を追加。重複時の「代替名で公開し直す」ボタンが確実に出るように。

## 0.2.43 - 2026-07-12

- 🔧 **AppRunの「アプリ作成上限」エラーに分かりやすい案内を表示**: 公開時に「Creation limit reached（アカウントのアプリ数上限）」で失敗した場合、名前変更では解決しないことと、コントロールパネルで不要なアプリを削除すればよいことを案内するように（コンパネへのリンク付き）。IDEでプロジェクトを削除しても公開済みアプリはアカウントに残る、という仕様との関係も明記。

## 0.2.42 - 2026-07-12

- 🐛 **プロジェクト削除後に「すべて消えた」ように見える問題を修正**: 開いていたプロジェクトを削除すると「ようこそ画面」に戻るが、この画面にプロジェクト一覧が無かったため、残っているプロジェクトが見えず全消去されたように見えていた（実際にはフォルダは無事）。ようこそ画面のサイドバーに**保存済みプロジェクトの一覧**（クリックで開く・🗑で削除）と「最近開いた場所」を表示するように。

## 0.2.41 - 2026-07-12

- 🔧 **プロジェクト削除を見つけやすく**: サイドバー上部のプロジェクト名をクリックして開くメニューで、🗑を常時表示に（従来はマウスを乗せたときだけ表示で気づけなかった）。さらにメニュー末尾に「🗑 このプロジェクトを削除…」を追加。
- 🔧 **新規プロジェクト作成時に「名前は後から変更できない」ことを明記**: あわせて「公開時の名前は公開のときに別途変更できる」ことも案内（v0.2.37で公開名が独立編集できるようになったため、フォルダ名の変更機能は事故リスクを考慮し提供しない方針）。

## 0.2.40 - 2026-07-12

- ✨ **プロジェクトの削除**: サイドバー上部のプロジェクト切替メニューで、各プロジェクトにマウスを乗せると出る🗑からプロジェクトを削除できるように。フォルダごと**ゴミ箱へ移動**するため、間違えてもFinderのゴミ箱から復元可能。公開済みのサイト・アプリとGitHubのバックアップはローカル削除の影響を受けない旨を確認ダイアログに明記（公開の停止は従来どおり③公開の各画面の「破棄」から）。削除したプロジェクトのタブ記録・「最近開いた場所」も自動で掃除。

## 0.2.39 - 2026-07-12

- ✨ **Claudeモードで画像をClaude自身が直接見るように（C2d）**: これまで画像を添付したターンだけは「視覚モデル（Qwen3-VL）で読み取り→本来のモデルで実行」の2段階処理（さくらのAI Engine）に切り替わっていたが、Claudeモード中はClaudeが画像を直接処理するように。スクリーンショットを貼って「ここを直して」という指示が、読み取り役を経由せず頭脳の視覚でそのまま伝わる。2段階処理はClaude未接続時（さくらのAI Engineのみ）の動作として存続。

## 0.2.38 - 2026-07-11

- ✨ **チャット履歴をプロジェクトのフォルダに保存**: IDEのチャット履歴は `<プロジェクト>/.sakuraide/chat.json`、単独チャットのセッションは `<ワークスペース>/.sakuraide/chats/chat-app.json` に保存されるように。プロジェクトのフォルダを移動・バックアップすると履歴も一緒に付いてくる。これまでのアプリ内部（localStorage）の履歴は初回起動時に自動で移行。書き込みはアトミック（保存中にクラッシュしても壊れない）。
- 🔒 **`.sakuraide/` は公開物・保存物から除外**: チャット履歴に秘密情報が混ざり得るため、HANAMII公開ZIP・AppRunイメージ・GitHub保存・環境変数検出・最終変更時刻の判定すべてから除外（履歴の保存が「コードが変更された」と誤認されて公開状況の古さ警告が出ることも防ぐ）。

## 0.2.37 - 2026-07-11

- 🐛 **【重要】Claudeモードの変更がエディタの古い内容で上書きされる問題を修正**: Claude（および実装委譲）がファイルを変更しても、エディタで開いていたタブは古い内容のまま残り、そのタブに入力するとオートセーブが古い内容でディスクを上書きし、**Claudeの変更が失われる**ことがあった（「更新したはずが初期状態に戻る」現象の原因）。Claudeがファイルを書き込んだ直後に該当タブをディスクから自動で読み直すよう修正。
- ✨ **公開名の重複をワンクリックで解決**: HANAMII/AppRunへの公開が「名前が既に使われている」で失敗したとき、他とかぶりにくい代替名（例: mysite-x7k2）を自動提案し、ボタン1つで名前を変えて公開し直せるように。AppRunの公開名（name）は②設定から✏️で直接編集も可能に（公開済みの場合はURLが変わる旨を確認）。公開失敗時のエラーにAPIの詳細メッセージを併記するようになり原因が分かりやすく。
- 🔧 **Claudeのモデル一覧を現行ラインナップに更新**: Claude Fable 5（最高性能）を追加、Sonnet 4.6 → Sonnet 5（〜8/31まで導入価格）に差し替え。旧モデルを選択済みの場合は自動で既定（Opus 4.8）に戻る。

## 0.2.36 - 2026-07-11

- ✨ **Claudeのモデルを選択可能に（C2c）**: Claudeモード有効時、チャット上部のモデル選択が Claude Opus 4.8（最高品質・既定）／Sonnet 4.6（バランス）／Haiku 4.5（低コスト・高速）の3択に切り替わる。コスト表示もモデル名入りに（🤖 Powered by Claude (Opus 4.8)・$0.1234）。
- ✨ **完全体モード: 実装をさくらのAI Engineへ委譲（C3）**: ClaudeとさくらのAI Engineの両方のキーが登録されているとき、Claudeが「まとまった実装作業」（新規ファイル作成・機械的な複数ファイル変更・テスト作成など）を低コストの Qwen3-Coder に委譲するツールを持つ。生成コードはClaudeを経由せず直接ファイルへ反映（🕘履歴スナップショット付き）されるため、**Claudeの高価なトークンを消費しない**。委譲結果はClaudeが検証し、2回失敗したら自分で実装に切り替える方針をシステムプロンプトで指示。委譲分のAI Engine利用量は既存の利用量・予算管理に積算。
- 🔧 チャット中の委譲は「🤝 さくらのAI Engineに実装を委譲しています…」と表示。

## 0.2.35 - 2026-07-11

- ✨ **ClaudeモードにIDE固有ツールを追加（C2b）**: Claudeが 🌐 Webページの取得（fetch_url）・📚 登録済み資料の検索（search_docs・さくらのAI Engineキーがある場合）・🌐 プレビューを開く（open_preview）を使えるように。参考サイトを見ながらの実装や、資料を根拠にした回答、作業後の自動プレビュー表示が可能に。実行の実体は従来のAI Engine経路と同一コード（挙動・安全対策も同じ）。
- 🔧 **右下のステータス表示がチャットの頭脳を反映**: Claudeモードが有効なときは「● Claude」、それ以外は「● さくらのAI Engine」を表示（認証情報の変更に連動）。
- 🔧 認証情報のClaude欄の説明文言を更新（「準備中」を撤去し、実際の動作を説明）。

## 0.2.34 - 2026-07-10

- ✨ **Claude頭脳モード（C2a・モードB成立）**: 認証情報に Claude（Anthropic API）のキーを登録すると、プロジェクトを開いたチャットの頭脳が Claude（claude-opus-4-8）に切り替わる。Claudeがファイルの閲覧・編集・コマンド実行（Read/Glob/Grep/Edit/Write/Bash）を自律的に行い、進行状況は従来同様「📄 ファイルを読んでいます…」等で表示。ターン末尾に「🤖 Powered by Claude・$0.0123」とコストを表示し、月別累計も記録。
  - **初回のみ同意確認**: コードと指示が Anthropic（米国）へ送信される旨を確認してから有効化（キャンセル時は従来のさくらのAI Engineで送信）
  - **安全網**: ファイル編集前に🕘履歴へ自動スナップショット（丸ごと戻せる）／危険コマンドは自動拒否／対応ツール以外は一律拒否
  - 画像添付時とプロジェクト未オープン時は従来どおり さくらのAI Engine が応答。`localStorage` の `sakura_claude_mode`=off で無効化可（設定UIは次版以降）
  - キーは使う瞬間だけ読み込み（暗号化保存・方式B）。会話の継続はセッション再開で実現

## 0.2.33 - 2026-07-10

- ✨ **Claude（Anthropic API）の認証情報を追加（C1・土台）**: ⚙️設定→「認証情報（APIキー）」に「Claude（Anthropic API）」を追加。キーはMacのキーチェーンで暗号化保存され、「🔌 接続テスト」で有効性を確認できる（利用可能なモデル件数を表示）。🔰取得手順ガイド付き（claude.aiのサブスクとは別体系である点も明記）。今後のバージョンで、Claudeを頭脳として使うモード（設計済み・C2）に使われる予定。
- 📦 **Claude Agent SDK を同梱**: 将来のClaude頭脳モードに向けた基盤。パッケージ版からSDKのネイティブバイナリが実行できることをスモークテストの恒久チェックとして追加（配布物のサイズが増加します）。

## 0.2.32 - 2026-07-07

- ✨ **💾 GitHubに保存**: タイトルバーの「💾」から、プロジェクトをGitHubのプライベートリポジトリへワンクリック保存（バックアップ・エンジニアへの受け渡し用）。初回はリポジトリを自動作成し、以降は「保存」だけ。ローカルにgitが無くても動作（GitHub API直）。`.env` などの秘密ファイルは自動除外。トークン（Fine-grained PAT）は認証情報で暗号化管理し、🔰取得手順ガイド付き。
- 🐛 **AppRun「公開の設定を作成」が失敗して操作不能に見える問題を修正**: 大文字や記号を含むプロジェクト名（HelloWorld等）でひな形作成が検証エラーになっていたのを、名前の自動正規化（小文字化・記号→ハイフン・3〜40文字）で解消。あわせて、エラー時に「作成」ボタンが消えて先へ進めなくなる表示分岐を修正（エラーを見ながら再試行できるように）。

## 0.2.31 - 2026-07-07

- ✨ **🕘 履歴（前の状態に戻す）**: AIがファイルを変更する前の状態を、送信1回ごとのまとまり（スナップショット・直近30世代）で自動保存。サイドバーの「🕘 履歴」から一覧（「7/6 21:15・3ファイル変更」）を開き、1クリックで「この時点に戻す」ことができる。復元前に現在の状態も自動で履歴へ退避されるため、**復元の取り消しも可能**。AIが作ったファイルの削除・上書きの巻き戻しに対応し、非エンジニアでも安心してAIに任せられる安全網。

## 0.2.30 - 2026-07-06

- 🔧 **内部整理（動作変更なし）**: 肥大化していたメインプロセス（main.ts 2,118行・IPC 82本）を責務別の12モジュール（src/main/ipc/）へ分割し、main.ts を276行に。IPCの総数・チャンネル名は分割前後で完全一致を検証済み。今後の機能追加・保守が安全かつ容易に。

## 0.2.29 - 2026-07-05

- 🔧 **公開実績のある公開先への切替はAIに聞かず即答**: 公開済みの実績がある公開先へ切り替えたときは、AI診断を行わず「✅ この公開先には公開済みの実績があります（7/4 13:59 公開）。今のコードのままで再公開できます」と決定的に案内するように（トークン消費もゼロ）。公開後にコードが変わっている場合はその旨を添える。AIによる適合診断は「実績が無い＝本当に答えが未知」の公開先に切り替えたときだけ実行される。実績があるのにAIが「⚠️ このままでは動きません」と誤判定する問題の根治。

## 0.2.28 - 2026-07-05

- 🐛 **AI適合チェックがAppRunの公開実績を見落とす問題を修正**: 公開記録の仕組み（0.2.26）より前に AppRun へ構築済みのプロジェクトで、「公開済みの実績」がAIに伝わらず「⚠️ このままでは動きません」と誤判定されることがあった。構築状態ファイル（.sakura-cloud）から実績を救済し、③公開の公開状況一覧にも「さくらのAppRun」行が構築日時付きで出るように。あわせて診断指示に「プロファイルの推奨事項や公開時の設定で吸収できる事柄（ポートの受け方等）は動かない根拠にしない」を明記。

## 0.2.27 - 2026-07-05

- 🔧 **公開先切替時のAI適合チェックを賢く**: (1) その公開先への公開実績（公開日時）を診断材料としてAIに渡し、実際に稼働中のプロジェクトへ根拠なく「対応が必要」と言わないように。(2) 結論を3段階（✅ そのまま動きます／✅ そのまま動きます（任意の改善あり）／⚠️ このままでは動きません）に分け、⚠️ は「動かない確実な根拠」がある場合のみに限定。「動くが理想形でない」点は任意の改善として簡潔に添える形に。

## 0.2.26 - 2026-07-05

- ✨ **公開状況の一覧表示**: ③公開を開くと冒頭に「📡 このプロジェクトの公開状況」を表示。公開先ごと（レンタルサーバ／AppRun／HANAMII）の公開日時とURLが一目で分かり、**公開後にコードを変更した公開先には「⚠️ その後に変更あり」**の警告が付く。複数の公開先を併用しているときの「どっちが古い？」を解消。
- ✨ **公開先切替時のAI適合チェック**: ワークフローバーで公開先を切り替えると、AIが自動でプロジェクトの構成を確認し「✅ そのまま動きます／⚠️ 対応が必要です」を報告。不足があれば既存コードの書き換えではなくファイル追加を優先した最小の提案をする（承認後に実行）。「あとで決める」への切替やAI作業中は従来どおりの案内のみ。
- 🐛 **公開先切替がAIに伝わらない問題を修正**: 同じプロジェクト内で公開先だけを変更したとき、AIへの前提知識（公開先プロファイル）が古いまま残っていた問題を修正。

## 0.2.25 - 2026-07-05

- 🐛 **「公開先 ▾」メニューが開かない問題を修正**: ワークフローバー右端の公開先切り替えボタンを押してもメニューが表示されなかった（ツールバーの横スクロール設定により、開いたメニューが縦方向に刈り取られて見えなくなっていた）。メニューをバーの外側に表示する構造に変更。

## 0.2.24 - 2026-07-04

- ✨ **HANAMII: アプリの健康状態を表示**: 公開パネルの状態表示に、実行中アプリの健康状態（ヘルスチェックの結果）を追加。正常なら「🩺 アプリの状態: 正常（○分前確認）」、異常なら「⚠️ アプリが応答していません」と詳細＋ログへの誘導を表示。デプロイの成否（公開済み/ビルド中/エラー）とは別の「いま動いているか」が IDE 内で分かるように。

## 0.2.23 - 2026-07-04

- ✨ **HANAMII: 公開名の指定**: 公開時の名前をプロジェクトフォルダ名と別に指定できる入力欄を追加。`test` のようなありふれた名前が HANAMII 内部で衝突（409 Application Already Exists）した場合に、フォルダをリネームせず名前を変えて再試行できる。公開済みプロジェクトの名前は変更不可（破棄→再公開で反映）。

## 0.2.22 - 2026-07-04

- ✨ **HANAMII: ヘルスチェック設定**: 公開パネルに「ヘルスチェック（任意）」を追加。アプリの死活監視パスを設定でき、既存プロジェクトへの変更・削除にも対応（公式APIの `PUT /health-check` を使用）。
- 🔧 **HANAMII: 環境変数の更新を正規経路に**: 再公開時の環境変数を、公式APIの `PATCH /env`（キー単位の上書き）で保存してから再デプロイする方式に変更。従来の「再デプロイのボディに同梱」（適用されるか不明だった）を廃止し、環境変数の変更が確実に反映されるように。
- ✨ **HANAMII: ログ表示と失敗原因の説明**: 公開済みプロジェクトに「📋 ログを見る」（最新100行・再取得ボタン付き）を追加。公開失敗時はエラーコードの日本語説明と管理画面へのリンクを表示。

## 0.2.21 - 2026-07-04

- ✨ **公開先の公式サイトリンク**: 新規プロジェクトの公開先選択（選択中の公開先の下）と、③公開の各公開先画面（レンタルサーバ／AppRun／HANAMII の説明ボックス）に「🌐 公式サイトを見る ↗」を追加。既定ブラウザで開く。URLは targetProfiles で一元管理し、トップに近い安定URLのみ採用。
- 🔧 **リンク切れの自動検知**: `npm run check:links` を追加し、週次の GitHub Actions（通常CIとは分離）で公式サイトURLの到達性を自動検査。リンク切れを利用者より先に検知する。

## 0.2.20 - 2026-07-04

- ✨ **HANAMII: 複数トークンの選択**: 認証情報に複数の HANAMII トークンを登録している場合、公開パネル内の「使うトークン」で切り替えられるように。切り替えるとワークスペース一覧もそのトークンで再取得。選択はプロジェクトに記憶され（トークンIDのみ・秘密値は保存しない）、次回同じトークンが既定になる。※ワークスペースは従来から複数対応（2件以上で選択式）。
- 🐛 **型エラーの修正と再発防止**: 新規プロジェクト画面の公開先追加時に混入した型エラーを修正。ビルドにレンダラの型チェックを組み込み、同種のすり抜けを防止（CIでも検査される）。

## 0.2.19 - 2026-07-04

- 🔧 **新規プロジェクトの公開先をグループ分け**: 「さくらインターネットのサービス」と「さくら以外の公開先」に分けて表示（③公開ダイアログと同じ構成）。Vercel など今後の公開先追加は「さくら以外」に自動で並ぶ。

## 0.2.18 - 2026-07-04

- 🐛 **新規プロジェクトの公開先に HANAMII を追加**: 「③公開」には対応済みだった HANAMII が、新規プロジェクト作成画面の公開先選択に出ていなかった漏れを修正（選択肢の末尾に追加・見出しを「公開先」に変更）。HANAMII を選んで生成すると、コンテナ実行前提（PORT環境変数・EXPOSE 8080・言語マニフェスト必須・永続データはコンテナ外）の構成でAIが生成する。

## 0.2.17 - 2026-07-04

- ✨ **🌐 Webから資料を作る**: 「📚 資料」に新タブを追加。Web検索（「さくらのナレッジから探す」絞り込み付き）→ チェックしたページだけ取得 → 内容を確認 → 資料としてアップロード、までを一続きで行える。作成される資料には出典URLと取得日時が記録され、ローカルの控えフォルダにも同じ内容を保存（「控えフォルダを開く」ボタン付き）。取得はユーザーが選んだページのみで、一括収集は行わない。Web検索には Tavily / Brave のAPIキー（認証情報で登録）が必要。

## 0.2.16 - 2026-07-04

- ✨ **📚 資料をチャットの根拠に**: 「📚 資料」に「このプロジェクトのチャットで資料を使う」設定を追加。有効にすると、送信内容に関連する資料があるときだけIDEが自動で抜粋を添付し、AIが資料を根拠に出典（資料名）付きで回答する（どのモデルでも機能）。ツール対応モデルは `search_docs` で資料を能動的に検索することもできる。タグで対象資料を絞り込み可能（複数タグはすべてを持つ資料のみ対象）。資料検索が失敗してもチャットは通常どおり続行する。

## 0.2.15 - 2026-07-04

- ✨ **📚 資料（AIに読ませる資料の管理）**: さくらのAI Engine の資料登録（RAG）機能に対応。タイトルバーの「📚」から、AIに事前に読ませたい資料（txt / pdf / html / docx / xlsx / md）を登録・一覧・タグ編集・削除できる。取り込み状況（🕐取り込み中 → ✅利用可能）を自動で追跡し、登録した資料へ「試し質問」もできる。アップロードした資料はさくらのクラウド（AI Engine）に保存され、APIキーは既存の「さくらのAI Engine」認証情報をそのまま使用（追加・削除は必ず利用者の操作で行い、AIが勝手にアップロードすることはない）。チャットが資料を根拠に回答する統合（自動注入・検索ツール）は次バージョン以降で対応予定。

## 0.2.14 - 2026-07-04

- 🔧 **IDEチャット: 画像添付時の2段階処理**: 画像を添付すると、これまでは画像対応モデル（ツール非対応）がターン全体を処理し、実コードを読まずに推測で提案→修正が実行されない問題があった。今後は「画像対応モデルが読み取りだけ→本来のツール対応モデルが読み取り結果を元に実コードを確認して実行」の2段階に（切替は「🖼 画像を読み取り、◯◯で実行します」と明示）。
- 🔧 **保守性**: チャット送信ロジックを ChatApp/ChatPanel の重複から共有フック `useAiChat` に統合。単体テスト基盤（Vitest 29件）とCI（GitHub Actions）を導入。

## 0.2.13 - 2026-07-03

- ✨ **HANAMII: 環境変数の自動検出**: プロジェクトのソースを走査し、コードが参照している環境変数（`process.env.X`／Python の `os.getenv`・`os.environ`／PHP の `getenv`・`$_ENV`／`.env(.example)` の記述）を検出して、公開パネルに「候補チップ」として表示。クリックで追加でき、`KEY`/`SECRET`/`TOKEN`/`PASS` 等を含む名前は自動で🔒シークレット既定に。値は読まずキー名のみを収集し、`PORT` など基盤が自動管理する変数は除外。

## 0.2.12 - 2026-07-03

- ✨ **HANAMII（国産PaaS）への公開に対応**: 「③公開」に HANAMII を追加。APIトークンを「認証情報」で一元管理し、プロジェクトをIDEがZIP化してアップロード → ワークスペース選択 → 公開URL表示 → 再公開／破棄まで。静的サイト（package.json 等のマニフェスト無し）は、最小の静的サーバと EXPOSE 付き Dockerfile を自動同梱してそのまま公開できる（利用者のファイルは変更せず、ZIP内にのみ追加）。
- ✨ **HANAMII: 環境変数の設定**: 公開時にアプリへ渡す環境変数（キー／値）を設定可能に。APIキー等の秘密の値は「シークレット」指定でき、端末には保存せず公開のたびに入力（通常値は `.sakuraide.json` に保存して再デプロイで再利用）。

## 0.2.11 - 2026-07-01

- ✨ **AppRun: 再デプロイ（PATCH更新）**: 既存アプリへの再公開を「作り直し」から「PATCH更新」に変更。公開URLを保ったまま最新の内容を新バージョンとして反映（バージョン履歴も保存）。
- ✨ **AppRun: 限定公開（IPアクセス制限）**: デプロイ済みアプリに、許可したIP/CIDRだけアクセスできる設定を追加（「今アクセスしている場所のIPを追加」ボタン付き）。独自ドメイン無しで「自分／社内だけに見せるプレビュー」が可能に。
- 🔧 **チャット: ツール非対応モデルの作業を自動で割り振り**: ツールを使えないモデル（Kimi 等）がファイル操作を求められた場合、その作業だけツール対応モデル（Qwen3-Coder）に切り替えて実際に実行する（切替は明示）。会話中は割り振りを維持し、無駄な再試行を省く。
- 🔧 **チャット: 応答状態を常に表示**: 「考えています…」を常時表示し、20秒応答が無ければ「⏳ 時間がかかっています…（⏹ で停止できます）」に切替。モデルがツール呼び出しの生マークアップを吐いて止まって見える問題も解消。
- 🐛 **公開の確認ダイアログ**: 実際に作成・更新される内容の一覧を表示し、下に何も無いのに「下の内容で…」と表示していた文言を修正。

## 0.2.10 - 2026-06-26

- 🔧 **起動の高速化**: エディタ（Monaco）を遅延読込（`React.lazy`）に変更し、ファイルを開くまで読み込まないように。初期バンドルを **3.9MB → 688KB** に削減、初回描画が約 1.6 秒から 1 秒未満へ短縮。あわせてウィンドウを `ready-to-show` まで非表示にし、起動直後の空ウィンドウのちらつきを解消。
- 🔧 **モデル選択を自前ドロップダウン化**: ネイティブ `<select>` は開く方向をブラウザが決めてしまい上方向に開くことがあったため、常に下方向に開くカスタムUIへ置き換え（外側クリック／Escで閉じる、選択中は✓表示）。
- 🔧 **モデルのおすすめマーク（⭐/⚠️）を撤去**: 適不適は作業内容によるため、断定的なマーク表示をやめた。あわせてフォールバックのモデル一覧と料金表を公式ページの最新単価で更新（提供終了モデルの削除、preview モデルを含む全単価の見直し）。
- 🐛 **チャットの吹き出し内テキストを左揃えに**: 利用者の発言吹き出しの中の文字が右揃えになっていたのを修正（吹き出しの位置は右のまま）。

## 0.2.9 - 2026-06-23

さくらのクラウド（AppRun）への公開機能の確立と、AIチャットの安定化・モデル運用整備を含む大規模アップデート。

- ✨ **クラウド公開（さくらのAppRun）一式**: アプリの宣言的な環境スペック → プラン生成 → 適用/破棄（TTL・ガードレール付き）までを実装。「③公開」フローに統合（独立した「クラウド」ボタンは廃止）。
- ✨ **コンテナレジストリの自動作成＋Docker不要ビルド**: 同梱の crane によるデーモンレスのイメージビルド／プッシュ。レジストリと最小権限プッシュユーザーを Cloud API 経由でIDEから自動作成（コントロールパネル不要）。公開後の AppRun 公開URLをIDEに表示。
- ✨ **コスト表示**: 予算・TTL のガードレールに加え、可能な範囲で実際の請求額を表示（公式 sacloud API の auth-status → bill/by-contract の2段取得）。
- ✨ **公開先の切替とAPIキー選択**: 公開先（ターゲット）別のデプロイプロファイルをAI文脈へ注入。AppRun公開時に使用するさくらのクラウドAPIキーを選択可能に。接続テストを3点（AppRun／レジストリ／請求）の権限プローブ化。
- ✨ **②試す: Python対応**: 不足ランタイムの導入を提案。
- 🔧 **AIチャットの安定化**: ツール呼び出しの取りこぼしによる無限ループ・推論文の暴走を修正。ツール非対応モデル（例: llm-jp）はツール無しで自動リトライ、小コンテキストモデルは max_tokens を自動調整。空の応答吹き出しを描画しないように。コピーボタンのフィードバック追加。
- 🔧 **Web検索をモデル非依存に**: IDE側が検索を実行して結果をAIへ注入する方式へ変更（どのモデルでも動作）。検索中は「🔍 Web検索中…」を表示。検索キー未設定時にAIが検索したふりをするのを防止。
- 🔧 **モデル運用の整備**: 提供モデルの差分チェック（`check:models`）と能力プローブ（`probe:models`）を追加。IDEとチャットで既定モデルを分離。
- 🔧 **未対応の公開先を非表示**: VPS・さくらのクラウド（IaaS）ターゲットは未実装のため一覧から隠した。
- 🐛 起動時の真っ黒画面（CloudModal の宣言前参照＝TDZ）を修正し、スモークテストがレンダラのエラーも捕捉するように改善。401認証エラー時に再発行を促す明確なメッセージを表示。

## 0.2.8 - 2026-06-16

- ✨ **レンタルサーバ（SSH）ファイル操作 MVP**: SSH経由でのファイル一覧・ダウンロード・アップロード（手動UI）。
- 📦 **ワンコマンド・リリーススクリプト**: バージョン更新 → dist → スモークテストを一括実行。

## 0.2.7 - 2026-06-16

- 🐛 AIによるプロジェクト生成で、コードフェンスや前置き文付きの応答だとJSONパースに失敗していたのを修正。

## 0.2.6 - 2026-06-14

- ✨ 同梱した第三者OSS（Electron/Chromium含む）のライセンス一覧をヘルプメニューから閲覧可能に。

## 0.2.5 - 2026-06-14

- 🔒 **セキュリティ強化**: 危険なコマンドの実行前確認、SSRF対策（プライベートIPブロック）、プロジェクト外パスへのアクセス制限、ガイドウィンドウの隔離。

## 0.2.4 - 2026-06-13

- ✨ 公開先（ターゲット）別のデプロイプロファイルをAI文脈に注入し、公開先を切り替え可能に。
- 📝 詳細なREADMEとIDE内の使い方ガイドを整備。

## 0.2.3 - 2026-06-13

- 🔄 初回起動の「セットアップ完了」ダイアログとカスタム dmg レイアウトを試験導入したが、不具合のため同日 0.2.4 で取り消し（利用者向けの実質的な変更なし）。

## 0.2.2 - 2026-06-13

- ✨ 公開準備ガイド・公開後フロー・使い方ガイド（Phase 3）を追加。

## 0.2.1 - 2026-06-12

- 🔧 UIの文言・ローディング表示・初心者向けガイダンスを改善。

## 0.2.0 - 2026-06-12

「作る・試す・公開」ワークフローを軸に再設計し、AIエージェント機能を大幅強化したリリース。

- ✨ **Webサイトをアプリと並ぶ第一級のプロジェクト種別に**。
- ✨ **AIのツール（関数呼び出し）対応**: Webページ読取、`list_files`/`read_file`（プロジェクト調査）、`write_file`（ファイル作成・上書き前バックアップ）、`run_command`/`open_preview`（自律デバッグ）。
- ✨ **Web検索ツール**: Brave / Tavily を優先度付きで選択可能。
- ✨ **画像（ビジョン）入力**、ドラッグ&ドロップでのファイル取り込み、開発サーバの停止/自動再起動。
- ✨ **書込み権限モード**（おまかせ/毎回確認）と公開前のAIセキュリティチェック。初回オンボーディングウィザードとAPIキー接続テスト。
- 📦 macOS配布用に electron-builder パッケージングを追加。
- 🔧 チャット履歴の送信量を上限管理（コスト抑制）。提供終了モデル選択時に最適モデルへ自動切替。出力を日本語に安定化。
- 🐛 gpt-oss の空応答、プロジェクト切替後の古い文脈での挨拶などを修正。

## 0.1.0 - 2026-06-09

初期リリース。さくらのAI Engine を使う macOS 向け IDE の土台。

- ✨ Monacoエディタ・ターミナル・ファイルツリーを備えた Electron IDE。
- ✨ さくらのAI Engine からモデルを動的取得。Qwen3-Coder を既定に。会話とコードを分離し、AIの出力をボタンでプロジェクトに適用。
- ✨ さくらのAppRun を公開先として追加。ワンクリック公開（レンサバ/AppRun）。
- ✨ 認証情報マネージャ（サービス別・複数キー対応）、APIキーごとの月間利用上限。
- ✨ HTMLプレビュー、プロジェクト切替、自動保存・終了時警告・セッション復元。
- 🔒 配布に向けたセキュリティ強化。さくらのAI Engine 呼び出しをメインプロセス経由にして CORS/接続エラーを解消。

